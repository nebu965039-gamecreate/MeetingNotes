*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/
.claude/

# リリース署名(秘匿情報)
keystore.properties
*.jks
*.keystore

# Firebase(Crashlytics/Analytics)。Firebase Console からプロジェクトごとに個別ダウンロードする
# ローカル専用ファイル(公開リポジトリのため、プロジェクト識別子を含むこのファイルはコミットしない)。
app/google-services.json

# デザイン作業用のスクショ(コミットしない)
docs/design/screens/

# JVM クラッシュダンプ(Kotlin デーモン等)
hs_err_pid*.log
replay_pid*.log
