# CLAUDE.md

Claude Codeがこのリポジトリで作業する際のガイド。ユーザー向けの説明は `README.md` を参照。

## プロジェクト概要

フリーランス・個人事業主向けの商談録音・要約Androidアプリ「商談メモ」。録音→オンデバイス音声認識で文字起こし→Claude APIで構造化要約(サマリー/決定事項/ToDo/次回打ち合わせ/懸念点)→クライアント別に保存、が中心機能。元の設計仕様書は `C:\projects\商談メモアプリ_設計仕様書.md`(このリポジトリの外にある)。

## 技術スタック

Kotlin 2.4.0 / Jetpack Compose(Material3、BOM 2026.08.00) / Navigation Compose / Room 2.8.4(KSP) / OkHttp+kotlinx.serialization(Retrofit不使用の手組みHTTP) / AGP 9.1.0 / play-services-ads 25.4.0。DIコンテナは使わず`MeetingNotesApp`(Applicationクラス)で手動DI。詳細は `README.md` の技術スタック表を参照。

## 重要なディレクトリ・ファイル

- `app/src/main/java/com/meetingnotes/MeetingNotesApp.kt` — DB/Repository/AdMob初期化の起点。新しいDAOやリポジトリメソッドを追加したら、ここでの配線漏れがないか確認する
- `app/src/main/java/com/meetingnotes/ui/MeetingViewModel.kt` — 録音〜要約〜保存の状態機械(`RecordingPhase`: Countdown/Recording/Stopping/Editing)。録音画面・結果画面の両方から参照される共有ViewModel(`Navigation.kt`のNavHost外側で1つだけ生成)
- `app/src/main/java/com/meetingnotes/data/remote/AnthropicClient.kt` — 要約クライアント。APIキーはアプリに持たず、`server/` の中継Worker(`SUMMARY_PROXY_URL`)へ `{transcript}` をPOSTする。Workerが Anthropic のレスポンスをそのまま返すためパース処理(`MessagesResponse`/`SummaryDto`)は不変。プロンプト・toolスキーマ・モデルは **Worker側(`server/src/index.ts`)** にある
- `server/` — 要約プロキシ(Cloudflare Worker、TypeScript)。デプロイ手順は `server/README.md`。秘密情報(`ANTHROPIC_API_KEY`, `APP_TOKEN`)は `wrangler secret` 管理でリポジトリに入らない
- `app/src/main/java/com/meetingnotes/data/MeetingRepository.kt` — 全DAOを束ねる単一リポジトリ。新機能を足す時はまずここにメソッドを足す
- `app/src/main/java/com/meetingnotes/data/local/MeetingNotesDatabase.kt` — Room DB定義。現在 version = 27(… v15 = `todos.isFollowupEmail`、v16 = `client_projects` テーブル + `meetings.projectId`(FK なし。削除時 `clearProject`)、v17 = `schedules` テーブル(独立した「予定」。1クライアントに複数)、v18 = `client_projects` に `phase`/`currency`/`estimatedAmount`/`wonAmount`/`wonAt`(案件の金額・状態)、v19 = `todos` 再構築(`meetingId` を nullable に・`clientId` NOT NULL + clients へ FK CASCADE を追加。手動 ToDo 対応。既存行の `clientId` は `meetings.clientId` から補完)、v20 = `client_projects.lostReason`(失注理由)、v21 = `client_projects` に `expectedCloseAt`/`probability`(想定クローズ日・受注確度)、v22 = `client_projects.phaseChangedAt`(よどみ検知。既存行は `createdAt` で補完)、v23 = `clients.followBoardSnoozedUntil`(ToDo ボードの定期フォロー スヌーズ)、v24 = `schedules` に `meetingUrl`/`location`(会議URL・場所)、v25 = `clients` に `leadSource`/`referredBy`(流入経路・紹介元)、v26 = `email_templates` テーブル(メール文面テンプレート)、v27 = `client_briefing` テーブル削除(「前回のおさらい」機能廃止)、v28 = `todos.snoozedUntil`(ToDo 一件ごとのスヌーズ再表示日時 epoch millis、nullable)。`feature/solo-crm` → `main` マージ済み)

## アーキテクチャ・設計上の重要事項

- **フォルダ/グループのパターンが2箇所ある**: 商談を整理する「フォルダ」(`FolderEntity`、クライアントに紐付く)と、クライアントを整理する「グループ」(`ClientGroupEntity`、独立エンティティ)。どちらも「手動作成→対象に1つだけ割り当て→デフォルト未展開の折りたたみUI→未分類/未展開は常時表示→削除時は対象を消さずSET_NULLで未分類に戻す」という同じ設計パターンを踏襲している。片方に機能を足すときはもう片方への横展開が必要か確認する(実際に「フォルダに名前変更・削除UIが無い」という抜けが過去に発生した)
- **DBスキーマ変更時は正式なMigrationを書く**(2026-09-01〜。以前の`fallbackToDestructiveMigration(true)`全面適用から変更した): `MeetingNotesApp.kt`は`fallbackToDestructiveMigrationFrom(dropAllTables = true, 1, 2, 3, 4)`のみ。**version 5(クローズドテスト初回配信)以降は破壊的フォールバックしない**ため、v5→v6以降の変更で`data/local/Migrations.kt`に`Migration`を書かないとアプリ更新時にクラッシュする(=書き忘れ防止)。手順: (1) Entity変更+`MeetingNotesDatabase`の`version`を+1 (2) ビルドで`app/schemas/<db>/<新version>.json`が生成される→**コミット** (3) `Migrations.kt`に`MIGRATION_x_y`を追加し`databaseMigrations`に含める (4) `app/src/androidTest`に`MigrationTestHelper`でテスト追加。`exportSchema = true`。v1〜v4は開発中・旧テストビルドのみなので破壊的マイグレーション許容
- **共有ViewModelのライフサイクル**: `MeetingViewModel`はNavHost外側で生成され画面をまたいで生存する。録音中の状態(カウントダウンJob等)を持つため、画面遷移で離脱する経路(戻るボタン等)では必ず`cancelRecordingFlow()`を呼んで後始末すること
- **エクスポートと画面表示の項目構成は揃える設計**: `MeetingExportContentBuilder.kt`(`ExportBlock` の共通モデル。PDF/Word/Markdown が共有)と`ui/common/MeetingSummarySections.kt`(画面表示)は意図的に同じ項目順序(サマリー→決定事項→懸念点・注意点→ToDo→次回打ち合わせ)を保つ。片方の順序を変えたらもう片方も確認する。エクスポート形式は `export/` に per-format のオブジェクト(`PdfExporter`/`WordExporter`/`MarkdownExporter`/`ExcelExporter`/`CsvExporter`/`IcsExporter`)。`ExcelExporter`/`CsvExporter` は `todos: List<TodoEntity>` のみを受け **ToDo 一覧(タスク/担当/期限/完了)だけ**を出力(xlsx シート名 "ToDo")。`IcsExporter` は `nextMeetingDate` が ISO 日付/日時のときだけ VEVENT を生成(未定なら null)。`MeetingDetailScreen` の `ExportOptionsDialog` は形式を3グループ(議事録ぜんぶ=PDF/Word/Markdown、ToDoリストだけ=Excel/CSV、次回打ち合わせ=.ics)で表示し、`IcsExporter.hasUsableDate(nextMeetingDate)` が false のときは .ics グループを非表示。いずれも Apache POI 不使用・標準ライブラリのみ(.docx/.xlsx は OOXML zip を手組み、CSV は RFC 4180 + UTF-8 BOM)
- **配色**: **2026-09-12〜カスタム `ColorScheme`(ネイビー基調)**: `ui/theme/NavyTheme.kt` の `navyLightColorScheme()`/`navyDarkColorScheme()`(引数なしの `lightColorScheme()`/`darkColorScheme()` に `primary`/`onPrimary`/`primaryContainer`/`onPrimaryContainer`/`inversePrimary`/`surfaceTint` だけを上書き。ライト = ネイビー `#1B2C4B`、ダーク = 明るめの青 `#8AB4F8` 系・同じ色相で明度を反転)。`secondary`/`tertiary`/`error` は M3 baseline のまま(下記「紫を使う場所を絞る」方針の一部・`DealPhaseChip` 以外のチップ等に紫が残るのは意図どおり)。**2026-09-12(同日追加)、中立面(`background`/`surface`/`surfaceVariant`/`surfaceContainer*`/`outline`/`outlineVariant`/`inverseSurface`/`inverseOnSurface`)も M3 baseline の紫みを帯びた既定値から置き換え**(ページ地 = `surface`/`surfaceContainer`/`surfaceContainerHigh` に採用する `--card`(白、ライト`#FFFFFF`/ダーク`#1A2233`)と、`background`/`surfaceVariant`/`surfaceContainerLowest`/`surfaceContainerLow` に採用するもう1トーンの2階調。`surfaceContainerHighest` は `--card-br`(ライト`#E3E8EE`/ダーク`#2B3550`)。「背景の色がタブ(下部ナビ)の背景と統一されていない」というフィードバックを受け、ページ全体の地と下部ナビ・カードの白を揃えた)。**2026-09-13、ページ地を「淡いグレー」から `FolderTabDefaults.sheetColor` と同じ淡いティール(ライト`#E3F0EE`/ダーク`#17302D`)に変更** — 最初は予定表画面だけ個別に teal-sheet 化したが、「この配色は全ての画面で共通にしたい」というフィードバックを受け、`background`/`surfaceVariant`/`surfaceContainerLowest`/`surfaceContainerLow` の値そのものをこのティールに変更してテーマ全体の既定にした(`ScheduleScreen` 個別の `containerColor` 指定は不要になったため撤去)。これにより ToDo/分析/クライアント詳細の `FolderTabDefaults.sheetColor` と、それ以外の全画面のページ地が完全に同じ色で統一される。**`ui/theme/AppBottomNav.kt`(下部ナビ)は `NavigationBar(containerColor = surface)`(白)+ `NavigationBarItemDefaults.colors(indicatorColor = surface)` で選択中タブの背景ピル(M3既定の`secondaryContainer`紫)を廃止、選択中はアイコン/文字の色(`primary`＝ネイビー)だけで示す**(Artifact「ネイビー基調のリデザイン案」の「クライアント詳細」同様、フッターメニュー地・項目地とも白に統一)。**`ui/theme/BrandNavy.kt`(固定のネイビー、`BrandNavy`/`OnBrandNavy`/`OnBrandNavyDim`/`OnBrandNavyMuted`。M3 の `primary`(ライト/ダークで明度反転)とは別物・ライト/ダーク共通の単色)を、実際に「濃いネイビーの帯」として見える箇所に適用**: `TabTopBar`(クライアント一覧/予定表/ToDo/分析の4タブ共通ヘッダー。地=`BrandNavy`、ホームボタン=薄い白丸〈`OnBrandNavyMuted`〉、タイトルアイコン=`CreateActionAmber`、下の罫線は廃止)/ `FolderTabRow`(ストリップ地を旧`surface`から`BrandNavy`に。ToDo・分析・`ClientDetailScreen`の3タブに反映。非選択タブの文字・件数ピルは`OnBrandNavyDim`系)/ `ClientDetailScreen`の2行ヘッダー/ `HomeDashboardCard`(カード地を`BrandNavy`に、`contentColor=OnBrandNavy`。凡例・成約率・パイプラインのラベルは`OnBrandNavyDim`、「売上・実績を見る」リンクは`CreateActionAmber`。中の`StatCell`3つは従来どおり`surface`色の白いチップのまま)。デザイン検討は Artifact「ネイビー基調のリデザイン案」(`https://claude.ai/code/artifact/3a29cb51-25e1-4ef4-b7a3-93fc53feb19b`)。**旧: M3 標準の紫パレット(引数なし baseline、カスタム `ColorScheme` 無し)**、以下は旧方針からの継続。**2026-09-09〜「紫を使う場所を絞る」方針(案A+B)**: (A) 一覧系画面(`ClientListScreen`/`ScheduleScreen`/`NotificationScreen`/`FollowupListScreen`/`ClientDetailScreen`)の TopAppBar は `primaryContainer` 配色を**やめて既定の `surface`** + 下に `HorizontalDivider(outlineVariant)`。タイトル左アイコンだけ `primary` tint。(B) セクションカードの塗り(`UpcomingBoard`/`FollowupBoard`/`FollowupListScreen` の内側パネル、`MeetingRow`/`ClientRow`/`ScheduleRow`/通知カード、下書き復元カード、`BriefingScreen` の hero)は `secondary/tertiaryContainer` → **`surfaceContainer`/`surfaceContainerHigh`(中立)**。区切り線は `onXxxContainer.copy(alpha)` → `outlineVariant`。**`primary` が残るのは**(2026-09-12〜ネイビー): 主要ボタン / リンク(下線) / 下部ナビの選択中ピル / `DealPhaseChip` 以外のチップ / ダッシュボードのアクセント / タイトルアイコン。`HelpScreen`・`RecordingScreen` は対象外(独自デザイン / 機能画面)。**`RecordingScreen`(2026-09-12〜13)**: カウントダウン・録音中フェーズは `Scaffold(containerColor = BrandNavy)` + アンバー(`CreateActionAmber`)のリング・タイマー・波形・スライドで停止(実装済み、下記「未完了のタスク」の一部は継続未実装)。**TopAppBar は 2026-09-13〜フェーズによらず常にネイビー**(旧: カウントダウン・録音中だけネイビーで、停止中/文字起こし中/編集画面は白いまま→「文字起こしテキストのヘッダーが白のまま」というフィードバックで統一。本文の背景は引き続き該当2フェーズのみネイビー、編集画面等は白背景のまま)。**2026-09-06〜ダークテーマ対応**: `MainActivity.kt` が `MeetingNotesApp.themeModeState`(`ui/theme/ThemeMode`: LIGHT/DARK/SYSTEM、`ui/theme/ThemePrefs.kt` で永続化)を読み `navyDarkColorScheme()`/`navyLightColorScheme()` を出し分けて `MaterialTheme(colorScheme = ...)` に渡す。設定は `ui/settings/SettingsScreen.kt`(「背景(テーマ)」行→ダイアログで3択)。`app.setThemeMode()` が Compose の `MutableState` を書き換えるため全画面へ即時反映される。例外として「作成/録音」系アクション(**クライアント一覧ヘッダーの `PersonAdd`/`CreateNewFolder`**(2026-09-10、クライアント追加 FAB は廃止しヘッダー右に移設)・**`ClientDetailScreen` アーカイブタブの `CreateNewFolder`**・**下部ナビの録音ボタン**(`AppBottomNav`))は `ui/theme/CreateActionColors.kt` の `CreateActionAmber`(#C5883B、2026-09-12 に `CreateActionBlue`(#1565C0) から変更。ライト/ダーク共通の単色は踏襲)。録音ボタンは背景 `primary`(ネイビー)+ 縁(`BorderStroke`)とアイコン tint・`PulsingHalo` の波紋色を `CreateActionAmber` に(旧: 背景/アイコンとも `primary`)。ホームのダッシュボード(ドーナツ)は `ui/theme/PhaseChartColors.kt`(`PhaseTagColors` 同系・ライト/ダーク明度入替) — これらは M3 パレットに従わない専用色。ヘルプ画面(`ui/help/HelpScreen.kt`)はカード + アイコン + セクション別アクセント色(紫/青/緑/スレート、`Accent*` 定数)のデザイン(ダークテーマでの見え方は未検証)
- **フォルダ/グループ作成の UI**: クライアント一覧・`ClientDetailScreen` アーカイブタブともに **`Icons.Filled.CreateNewFolder`(青 tint)**。片方の見た目を変えたらもう片方も揃える。
- **`ClientDetailScreen` は3タブ構成(2026-09-09。2026-09-11 に `PrimaryTabRow` → `FolderTabRow` へ・下部ナビ追加)**: `topBar` = `Column { TopAppBar(タイトル=クライアント名 maxLines=2, actions=⋮) + FolderTabRow(アーカイブ / ToDo(件数) / 情報) }`。コンテンツ領域は `.background(FolderTabDefaults.sheetColor)`。⋮ メニュー = プロジェクトを管理 / クライアントを削除。**`bottomBar` = `Column { BannerAdView() + AppBottomNav(currentTab = null) }`**(2026-09-11、`Scaffold(contentWindowInsets = WindowInsets(0))`)。`ClientDetailScreen` に `onSwitchTab: (MainTab)->Unit` / `onStartRecording` 引数。タブ切替は `Navigation.kt` が `navController.getBackStackEntry(Routes.MAIN).savedStateHandle["pendingTab"] = tab.name` + `popBackStack(MAIN)` → `MainTabsShell(savedStateHandle=)` の `LaunchedEffect` が拾って `switchTab`。
  - **アーカイブ**タブ: 商談一覧。ヘッダー行に「アーカイブ」+ 検索 / `CreateNewFolder`(青) / 並び替え をまとめる。検索・フォルダ折りたたみ・`MeetingRow`(`DealPhaseChip`)は従来どおり。
  - **ToDo**タブ(`TodoTab`): 「ToDo」「完了」の2サブタブ(`PrimaryTabRow`)。行は共有 `ui/common/TodoRow.kt`(`Checkbox` で完了/復活、`😴 M/d まで` チップ、行タップは商談由来→商談詳細・手動→編集ダイアログ)。ToDo = `ClientDetailViewModel.openTodos`(スヌーズ中は末尾)、完了 = `.doneTodos`(id 降順、打消し線)。タブラベルに件数。**ToDo サブタブの直下にツール行**(2026-09-10): 左に並び替え(`TodoSortOrder`: `DUE_DATE`「期限順」/`CREATED`「作成日順」= id 降順。`MeetingArchiveSearch.kt` に enum、`viewModel.todoSort`/`setTodoSort`)、右に「ToDoを追加」。各 ToDo 行の trailing に `SnoozeMenu`(`viewModel.snoozeTodo`/`unsnoozeTodo`、`todos.snoozedUntil`)。
  - **情報**タブ: `ClientInfoContent`(`ClientInfoScreen.kt`、旧スタンドアロン `ClientInfoScreen` から抽出)。名前/グループ/現在のステータス(`DealPhaseChip`)/メール/電話/**流入経路・紹介元(v25、入力があれば)**/担当者/備考の閲覧。**編集導線は右上の `SmallFloatingActionButton`(鉛筆アイコン、`Box` オーバーレイ)**(2026-09-10、旧「情報を編集」ボタン廃止)→ `ClientEditScreen`(`Routes.CLIENT_EDIT`)。旧 `Routes.CLIENT_INFO` は廃止。`ClientEditScreen` は流入経路(`LEAD_SOURCES` プリセット quick-fill + 自由入力)と紹介元欄も持つ。`ClientInfoViewModel.saveInfo` が `updateClientInfo` + `moveClientToGroup` + `updateClientLeadInfo` を呼ぶ。
  - `bottomBar` = `BannerAdView(navigationBarsPadding)` のみ(2026-09-09、録音ボタン撤去)。`ClientDetailScreen` の引数は `onOpenClientInfo` → `onEditClient` に変更。
- **クライアント配下のプロジェクト(= 案件、任意、2026-09-09、DB v16 / 金額は v18)**: 1クライアントに 0..N 個。`client_projects` テーブル + `meetings.projectId`(FK なし)。`ClientProjectDao` / `MeetingRepository.observeClientProjects` / `observeAllProjects` / `addClientProject` / `updateClientProject` / `deleteClientProject`(先に `clearProject` で商談の projectId を null 化)/ `setMeetingProject`。
  - **案件の金額・状態(v18)**: `client_projects` に `phase`(`DealPhase.wireValue` or null)/ `currency`(`"JPY"`/`"USD"`、`util/Money.kt` の `Currency` enum。小数は扱わない)/ `estimatedAmount`(見積額 Long?)/ `wonAmount`(成約額 Long?)/ `wonAt`(成約日 epoch millis)。**入力は `ProjectFormDialog`(案件フォーム)からのみ手動**(フェーズ変更プロンプト等は無し)。`wonAt` は `phase == WON` のとき自動補完(フォームで手動指定があればそれを優先。`addClientProject`/`updateClientProject` 内で解決)。`util/Money.kt` = `formatMoney(Long?, Currency|code)` / `parseMoneyInput(String)`。
  - **失注理由(v20)**: `client_projects.lostReason`(String?、自由記述 or プリセット文言)。`phase == LOST` のときのみ `ProjectFormDialog` に「失注理由」欄(`LabeledDropdownField` のプリセット quick-fill〈`LOST_REASON_PRESETS`〉+ 自由入力 `OutlinedTextField`)。`addClientProject`/`updateClientProject` は `phase != LOST` なら null にする。`ManageProjectsDialog` の一覧・情報タブの案件セクションに表示。**売上ビュー**は `SalesRules` が `lostReasonBreakdown`(理由→件数・降順)を集計し `SalesScreen` に「失注理由の内訳(全期間)」として表示。
  - **想定クローズ日・受注確度(v21)**: `client_projects.expectedCloseAt`(epoch millis?)/ `probability`(Int? 0..100)。`DealPhase` に `defaultProbability`(10/20/40/60/75/100/15/0)。`ProjectFormDialog` は **進行中フェーズ(`isActive`)のとき**「想定クローズ日」ボタン + 「受注確度(%)」欄(未入力プレースホルダ = フェーズ既定値)。`addClientProject`/`updateClientProject` は非 active フェーズなら両方 null、`probability` は `coerceIn(0,100)`。実効確度 = `probability ?: phase.defaultProbability`。`ClientDetailScreen.projectForecastLabel` が一覧行に「確度 X% ・ 想定クローズ M/d」。**売上予測** = `SalesRules.weightedPipeline` = Σ(見積額 × 実効確度)、`SalesScreen` サマリーに「売上予測(加重)」。※フル forecast ビュー(月次予測・目標対比)は Billing 後の Pro 拡充で。
  - `ClientDetailViewModel`: `projects` / `projectFilter`(`null`=すべて / `NO_PROJECT`(-1L)=プロジェクトなし / それ以外=ID)。`sortedMeetings`・`searchResults`(`applyProjectFilter`)に加え **`openTodos`・`doneTodos` も `_projectFilter` で絞り込む**(`meetingProjectById` で商談→プロジェクトを引き当て、`matchesProject` 共通判定)。CRUD メソッドあり(`addProject`/`updateProject`/`deleteProject`、金額・フェーズ・成約日を引数に取る)。
  - UI: `projects` が1件以上のとき **タブ直下**に `ProjectFilterBar`(2026-09-11、`TextButton` + `DropdownMenu` のプルダウン。旧 `LazyRow`+`FilterChip` から戻した。項目 = **「すべてのプロジェクト (N)」/ 各プロジェクト (N) / 「未定のプロジェクト (N)」(=プロジェクトなし、`NO_PROJECT`。該当商談が1件以上のときだけ)**。件数は絞り込み前の全商談から `ClientDetailViewModel.projectMeetingCounts`〈projectId(null=なし)→件数〉)を表示し **アーカイブ・ToDo 両タブに効く**(情報タブでは非表示。解除バーは廃止・プルダウンで「すべてのプロジェクト」を選ぶ)。**案C: `MeetingRow` の `・📁 案件名` は絞り込み中でないときリンク(下線・primary、`onProjectClick` = `setProjectFilter(projectId)`)。すでにその案件で絞り込み中なら非リンク**。⋮ メニュー「プロジェクトを管理」→ `ManageProjectsDialog`(一覧に名前+`DealPhaseChip`+金額サマリ、行タップで `ProjectFormDialog` 編集 / 「案件を追加」/ 削除アイコン→`ConfirmDialog`)。`ProjectFormDialog` = 案件名 + 進捗/通貨の `LabeledDropdownField` + 見積額/成約額の数値入力 + `phase==WON` のとき成約日ボタン(`NextMeetingDateTimeDialog` 再利用)。`MeetingRow` の ⋮ に「プロジェクトを変更」→ `ProjectPickerDialog`。
  - **情報タブ(`ClientInfoContent`)**: 案件が1件以上のとき「案件」セクション(各案件の名前+`DealPhaseChip`+見積/成約額、通貨ごとの合計)。`ClientInfoViewModel.projects`。
- **フェーズ体系(2026-09-11 に「案件フェーズ = 正」へ寄せた)**:
  - **案件フェーズ** `client_projects.phase` = **クライアントの現在の状態の正**。設定 = `ProjectFormDialog`(案件フォーム。`ManageProjectsDialog` から)/ **要約結果画面の保存後プロンプト**(下記)。`ui/client/ClientDealPhaseRules.of(projects)` = 代表フェーズ(進行中案件の最先端〈`DealPhase.funnelRank`〉/ 無ければ WON→LOST / 案件0なら null)。`MeetingRepository.observeClientDealPhase(): Flow<Map<Long,DealPhase>>`。使われる場所: **クライアント情報タブのステータス**(`ClientInfoViewModel.latestPhase`)/ **ホームのドーナツ・成約失注**(`HomeViewModel`、進行中「案件」をフェーズ別に数える)/ `FollowupRules` の表示フェーズ / 売上分析 / 停滞案件 / 金額集計。
  - **商談フェーズ** `meetings.dealPhase`(AI 推定)+ `meetings.phaseOverride`(手動)= **その商談時点のスナップショット**。アーカイブ / 商談詳細の `DealPhaseChip` に表示・手動修正可(過去の記録)。**クライアントの「現在」には使わない**。`observeLatestMeetingPerClient` は `lastRecordedAt` / `meetingId`(リンク用)にのみ使用。
  - **連動 = 保存後プロンプト**(`ResultScreen.PostSavePromptDialog`、`MeetingViewModel.PostSavePrompt`): 要約保存直後、(a) クライアントに案件が0件なら「案件を登録しますか?」(案件名 + フェーズ〈AI推定を既定〉、「あとで」で空登録=スキップ)、(b) 進行中案件がちょうど1件 & AI推定フェーズ ≠ 現在フェーズなら「案件フェーズを更新しますか? 〈現在〉→〈AI〉」(ドロップダウンで失注/保留も選べる)。`MeetingViewModel.saveMeeting*` は `onSaved` を取らず `_savedClientId`/`_postSavePrompt` をセット、`ResultScreen` の `LaunchedEffect` がプロンプト解決後に `onSaved` を呼ぶ。`MeetingRepository.setProjectPhase`。
  - **移行**: `MeetingRepository.backfillClientProjects()`(`MeetingNotesApp`、`app_migrations` の `client_projects_backfilled`)= 商談はあるが案件が無いクライアントへ案件「案件」を1件作成(フェーズ=最新商談の実効フェーズ)。以降の新規クライアントは初回商談保存時のプロンプトで案件作成。
  - **パイプラインボード(`ui/pipeline/`)は 2026-09-11 に廃止**(`Routes.PIPELINE` / `onOpenPipeline` / `setClientProjectPhase` も削除)。
  - **分析画面(2026-09-09、`ui/analytics/` + `ui/sales/`)**: 下部ナビの「分析」タブ(`AnalyticsScreen`)。ヘッダーは `TabTopBar`(2026-09-10、他タブ画面と統一)、タブは `FolderTabRow` = 売上 / 活動 / 顧客 / フォロー。画面ごと `ProGate`(`ProAccess.gatingEnabled && !isPro` のとき `ProLockedContent` + `ProPaywallDialog`。現状 gating OFF)。
    - **売上**タブ = `SalesReportTab(repository)`(旧 `SalesScreen` の中身を抽出。期間セレクタ + 通貨ごとの `CurrencyReportCard`)。`SalesRules` 由来: 月次成約額 / 期間内の成約額・件数・平均単価 / 全期間の成約率(金額・件数)/ 売上予測(加重)/ セールスサイクル(`avgCycleDays` / `avgDaysInPhase`)/ 失注理由の内訳 / フェーズ別パイプライン。
    - **活動 / 顧客 / フォロー**タブ = `AnalyticsRules`(純粋関数 `object`、`AnalyticsRulesTest`)+ `AnalyticsViewModel`(`observeAllMeetings`/`observeClients`/`observeAllProjects`/`observeAllTodos` を combine)。活動 = 月次商談数(棒)・今月/前月比・対面/リモート比率・平均/合計録音時間。顧客 = 月次新規クライアント数・流入経路の内訳・流入経路別の成約率(clients×projects の join)・紹介元ランキング。フォロー = ToDo 完了率・期限切れ件数・お礼メール送信率・平均フォロー日数(録音→`followedUpAt`)。
    - `MeetingDao.observeAll()` / `TodoDao.observeAll()` を新設。`ui/common/ProLockedContent`(旧 `SalesScreen.LockedContent` を汎用化)。
    - **グラフの色(2026-09-10)**: `ui/theme/AnalyticsChartColors` の**タブ別**関数(M3 紫と分離するための専用色。ライト/ダーク明度入替)。活動 & 売上 = `activity`(ティール `#0D9488`/`#4FD1C5`)、顧客 = `customers`(インディゴ `#4F46E5`/`#A5B4FC`)、フォロータブは**カード別**で「ToDo の消化」= `followTodo`(アンバー `#C2740C`/`#E5C37D`)・「フォローアップ」= `followEmail`(バイオレット `#7C3AED`/`#C4B5FD`)。`AnalyticsScreen` がタブ番号で色を選び `MonthlyTrend`/`HBar` に渡す。`AnalyticsScreen` の `MiniBars` は廃止、`MonthlyTrend`(上に「今月 N / 前月比 / 月平均」の要約 + 各バー上に数値 + 横軸「M月」+ 最新月をアウトライン強調)に置換。活動タブの「今月の商談」`StatCard` は要約と重複するため削除。`HBar` は `color` 引数を取る。`SalesScreen.MonthlyBarChart` の色は `AnalyticsChartColors.bar`(=ティール)、月ラベルは `yy年M月`。
    - **見出しの視認性改善(2026-09-13)**: 「題目とデータのフォント・色が似ていて見づらい」というフィードバックを受け、`AnalyticsScreen.StatCard` と `SalesScreen.SectionLabel`(いずれも `labelMedium`+`onSurfaceVariant` の地味な見出しだった)を `ui/common/SectionHeading`(ネイビー縦バー+`titleMedium`)に置き換え。カード内の行ラベル(`KeyValueRow`/`Stat`、`labelSmall`〜`bodyMedium`+`onSurfaceVariant`)はそのまま=見出しと行ラベルの間に明確な強弱がつく。
  - **(旧記述)売上・実績ビュー(Pro、2026-09-09、`ui/sales/`)**: `Routes.SALES`。ホームの `HomeDashboardCard` 下部「売上・実績を見る ›」(`onOpenSales`)から遷移。`SalesRules`(純粋関数の `object`、`SalesRulesTest` 対象)が `observeAllProjects()` を **通貨ごとの `SalesCurrencyReport`** に集計: 直近12ヶ月の月次成約額 / 期間内(今月・今年・全期間、`SalesPeriod`)の成約額・件数・平均単価 / 全期間の成約率(金額 = 成約額 ÷ (成約額+失注見積額)、件数 = 成約数 ÷ (成約数+失注数))/ 売上予測(`weightedPipeline` = Σ 見積額×実効確度)/ **セールスサイクル**(`avgCycleDays` = 案件作成→成約の平均日数、`avgDaysInPhase` = 進行中案件のフェーズ別 平均滞留日数 = now − `phaseChangedAt`)/ 失注理由の内訳(`lostReasonBreakdown`)/ 進行中案件のフェーズ別パイプライン。`SalesViewModel` は `_period` と `observeAllProjects()` を combine。`SalesScreen` = TopAppBar + `SingleChoiceSegmentedButtonRow`(期間)+ 通貨ごとの Card(`Canvas` 手描き月次棒グラフ + サマリー + フェーズ別パイプラインの横棒)。**`ProAccess.shouldLock`(現状 false)が true のときは `LockedContent` + `ProPaywallDialog` を出すだけ**(Billing 実装後に有効化)。棒グラフ色は `PhaseChartColors.of(WON, darkTheme)`。
- **アーカイブの検索・並び替え**: `ui/client/MeetingArchiveSearch.kt`(純粋関数の `object`、単体テスト対象)に集約。検索は LIKE 相当のメモリ内部分一致(タイトル→サマリー→決定事項→懸念点→ToDo→文字起こし全文 の優先順で最初の一致をラベル付きプレビュー表示)。FTS は不採用(個人利用の件数では LIKE で十分・日本語の途中一致が確実)。ToDo 検索用に `TodoDao.observeByClient`(meetings と JOIN)を追加。並び替え(`MeetingSortOrder`)は日本語 `Collator`。並び替え設定はメモリ保持(画面離脱でリセット)。検索中はフォルダ表示をやめフラットな結果リストにする
- **下部ナビ + 録音FAB(2026-09-08、`ui/home/MainTabs.kt`。2026-09-09 にホームをナビから外し「分析」を追加)**: `Routes.MAIN` = `MainTabsShell`(NavHost の start destination)。内側 `NavHost`(`tabNav`)は **ホーム(`HOME_ROUTE = "tab_home"`、start destination・下部ナビには出さない)+ 4タブ(クライアント/予定表/ToDo/分析)**。詳細画面(クライアント詳細・商談詳細・録音・設定・ヘルプ・通知・パイプライン)は外側 `navController` へ委譲する。**ホームへは各タブ画面の左上ホームアイコン(`onHome` = `goHome()`)から戻る**(`ClientListScreen`/`ScheduleScreen`/`FollowupListScreen`/`AnalyticsScreen` の `TopAppBar.navigationIcon`)。`MainTab` enum は CLIENTS/SCHEDULE/TODO/ANALYTICS の4つ(HOME を削除)。ホーム表示中は下部ナビの選択項目なし。ホームの「売上・実績を見る」は `switchTab(MainTab.ANALYTICS)`。旧 `Routes.SALES` / 独立 `SalesScreen` は廃止(`SalesReportTab` として分析画面の「売上」タブに吸収)。`Scaffold(contentWindowInsets = WindowInsets(0))` の `bottomBar` = `Column { BannerAdView(); AppBottomNav(...) }`。**下部ナビ本体は `ui/home/AppBottomNav.kt` に切り出し(2026-09-11)**、`MainTabsShell` と **`ClientDetailScreen`** で共有(バナー広告は各 `bottomBar` に1つずつ)。`AppBottomNav(currentTab: MainTab?, todoBadge, onSelectTab, onStartRecording)` = 4タブ + 中央の空きスロット + 丸い録音ボタン overlay。**下部ナビの並び(2026-09-09)= 予定表 / クライアント / ［録音］ / ToDo / 分析**。**クライアント詳細から録音ボタンを押すとグローバル録音(クライアント未指定)**、タブを押すと `onSwitchTab` → `savedStateHandle["pendingTab"]` 経由で `MainTabsShell` がそのタブへ。**クライアント詳細画面からは(旧仕様の)録音導線を撤去**(2026-09-09、`onShowBriefing` 引数削除。※2026-09-11 に下部ナビ経由の録音ボタンは復活)。**4タブ画面のヘッダーは `ui/common/TabTopBar`(左上「ホーム」ボタン + タイトル左アイコン primary + 罫線)で統一**(`ClientListScreen`=People / `ScheduleScreen`=CalendarMonth / `FollowupListScreen`=CheckCircle / `AnalyticsScreen`=BarChart)。`ui/common/PulsingHalo`(静的リング + 控えめな波紋1本。OS のアニメ OFF なら静的のみ)で囲む。**全タブから押せる**(`onStartRecording` = クライアント未指定録音)。ToDo タブに `Badge`(`TodoDao.observeOpenTodoTotal`)。`NavigationBarItem` は `RowScope` 拡張 `NavTab`(ラベル 10sp / `maxLines=1` / `softWrap=false` で「クライアント」の改行を防ぐ)。カウントダウン画面(`RecordingScreen.CountdownContent`)も `PulsingHalo`(やや強め)+ `AnimatedContent` で数字をティックごとにポップ。`ClientListScreen`/`ScheduleScreen`/`FollowupListScreen` は **戻る矢印を撤去**(タブなので)、`onBack` 引数も削除、`contentWindowInsets = WindowInsets(0)`(ヘッダー配色は 2026-09-09 の配色見直しで `surface` + 罫線に。上記「配色」項参照)。旧 `Routes.HOME`/`CLIENT_LIST`/`SCHEDULE`/`FOLLOWUP_LIST` は廃止。`onClientDeleted`・`ResultScreen.onSaved`(直接録音フロー)は `popBackStack(Routes.MAIN)` へ。
- **ホーム画面(2026-09-05〜、`ui/home/HomeScreen.kt` + `HomeViewModel.kt`。2026-09-08 に下部ナビ導入で再構成、2026-09-12 に「ToDo」ボード廃止・並び順変更)**: 旧来は `ClientListScreen` がアプリ起動直後だったが `HomeScreen` に置き換え。**構成・並び順(2026-09-12〜)**: ヘッダー(日付+挨拶+**通知ベル**〈未読で `Badge`〉+設定+ヘルプ)→ **ダッシュボード**(`HomeDashboardCard`、最上部固定・ネイビー)→ 下書き復元カード(あれば)→ **直近の予定**(`UpcomingBoard`)→ **期限切れ**(`OverdueTodoBoard`、非空時)→ **3日以内のToDo**(`DueSoonTodoBoard`、非空時)→ 動いていない案件(あれば)。**アクションタイル(2×2)は廃止**(録音=FAB、クライアント一覧/予定表=下部ナビ、通知=ヘッダーベル)。**「進行中のフェーズ」セクションは削除(2026-09-09)** — ダッシュボードのドーナツと重複するため。`HomeViewModel.phaseCounts` 公開フローも撤去(`phaseCountsOf` はダッシュボードで継続使用)。`onOpenSchedule` は下部ナビのタブ切替(`switchTab`)に配線。
  - **ホームの「ToDo」ボード廃止(2026-09-12)**: `ui/client/FollowupBoard.kt`(`FollowupBoard` composable / `FollowupRules` / `FollowupItem` / `FollowupRulesTest`)を全削除、`HomeViewModel.followups` と `HomeScreen` の `onOpenFollowupList` 引数(`MainTabs.kt` の配線含む)も削除。**理由**: 「未完了 ToDo が1件でもあるクライアント」を出すだけの単純なクライアント別カードで、下記「期限切れ・3日以内のToDo」(個々のタスク単位)や下部ナビ ToDo タブ(`FollowupListScreen`、全件を個々のタスク単位で確認可)と条件が異なるまま両方ホームに並んでいたため、件数の食い違いが分かりにくいというフィードバックを受けて撤去。`MeetingRepository.observeOpenTodoCountByClient`/`observeClientDealPhase` はこのボード専用ではない汎用の集計メソッドのため残置(現状呼び出し元なし)。
  - **「やること(期限あり)」→「期限切れ」「3日以内のToDo」の2枚に分割(2026-09-12、旧称「やること(期限あり)」→「期限切れ・3日以内のToDo」からさらに分割)**: `ui/home/DueTodoBoard.kt` は `OverdueTodoBoard`/`DueSoonTodoBoard`(共通実装 `DueTodoBoardCard` を内部で共有)の2つの public 関数に。`HomeViewModel.dueTodosSource`(旧 `dueTodos`、期限切れ+3日以内をまとめた内部専用の中間 Flow)から `overdueTodos`(`dueDate < 今日`)/`dueSoonTodos`(`dueDate >= 今日`)へ分岐。各カードは**最大5件**表示(`MAX_VISIBLE`)、見出し右に**「すべて表示」リンク**があり、`HomeScreen` の `onOpenTodo: (TodoDueFilter) -> Unit` 経由で ToDo タブへ該当フィルタ(`OVERDUE`/`DUE_SOON`)を渡して遷移する(下記「ToDo タブの期限フィルタ」参照)。
  - **動いていない案件(2026-09-09、DB v22)**: `StaleDealRules.compute`(純粋関数、`StaleDealRulesTest`)= 進行中(`DealPhase.isActive`)なのに `phaseChangedAt`(なければ `createdAt`)から `STALE_DAYS`(21日)以上フェーズが動いていない案件を停滞日数の降順で返す。`HomeViewModel.staleDeals`(`observeAllProjects` + `observeClients`)。`StaleDealBoard`(`ui/home/`)は現在ホームの**最後**に、非空のときだけ表示(2026-09-12、直近の予定・期限切れ・3日以内のToDo の後ろへ移動)。行タップ → クライアント詳細。`phaseChangedAt` は `addClientProject`(= 作成時刻)・`updateClientProject`(フェーズ wireValue が変わったときだけ現在時刻に更新)で管理。
  - **ToDo タブの期限フィルタ・Home↔ToDo間の受け渡し(2026-09-12)**: `ui/common/TodoDueFilter.kt`(`ALL`/`DUE_SOON`/`OVERDUE` の enum + `matchesDueFilter(dueDate, filter, today)` 純粋関数)を新設。`MainTabsShell` が `pendingTodoFilter`(`remember { mutableStateOf<TodoDueFilter?>(null) }`)を保持し、`HomeScreen` の「すべて表示」→ `onOpenTodo(filter)` で `pendingTodoFilter` をセットしてから `switchTab(MainTab.TODO)`。`FollowupListScreen` は `initialFilter: TodoDueFilter?` を受け取り `LaunchedEffect(initialFilter)` で `viewModel.setDueFilter(it)` + 「ToDo」サブタブへ強制切替。`MainTabsShell` 側は直後の `LaunchedEffect(pendingTodoFilter)` で `null` に戻す(再訪時に勝手に再適用しない)。同じホーム内の 2 NavHost(HOME_ROUTE と TODO_ROUTE)がどちらも `tabNav` 配下のため、外側 `SavedStateHandle` 越しの `pendingTab` ブリッジ(クライアント詳細→タブ切替用)とは別に、素の `remember` state で十分。
  - **`HomeDashboardCard`**(`ui/home/HomeDashboard.kt`): 進行中フェーズのドーナツ(`Canvas` の `drawArc` 手描き、ライブラリ不要)+ 中央「進行中 N」+ 凡例、下に数字3つ(クライアント/今月の商談/未完了ToDo、ToDo>0 はエラー色)、区切り線の下に **全期間の成約率**(成約 ÷ (成約+失注)、母数0なら `winRate == null` で行ごと非表示)。案件に金額があれば最下部に **今月の成約 / パイプライン**(通貨ごと、`AmountRow`。`wonAmountThisMonth` = `phase==won` かつ `wonAt` が今月 / `pipelineAmount` = 進行中案件の見積額合計)。**表示のみ・タップ動作なし**。`HomeViewModel.dashboard`(`HomeDashboard` data class、`combine(clients, observeMeetingCountSince(月初), observeOpenTodoTotal, latestMeetings, observeAllProjects)`)。
  - **色**: ドーナツ・ファネルとも `ui/theme/PhaseChartColors.kt`(`of(phase, darkTheme)`。`PhaseTagColors` と同系色相・彩度高め・ライト/ダーク明度入替)。旧 `PhaseTrackerColors.kt` は削除。
  - 共通事項: リンク文字は `TextDecoration.Underline`(件数表示と区別)。日付ラベル(本日/明日/M-d)は `ui/common/DateLabels.kt` の `relativeDateTimeLabel`。`DueTodoBoard` の内側は `surface`(白)の角丸パネル + 丸チェックアイコン。`UpcomingBoard` の内側は `secondaryContainer` パネル + 日時ラベル(上)/クライアント名(下)+ `DealPhaseChip` + `ChevronRight` + 区切り線、プレビュー3件。**行タップは `ScheduleDetailDialog`(2026-09-11、旧: クライアント画面)**。
- **直接録音フロー(2026-09-06〜、下部ナビ導入後は録音FABが起点)**: FAB → `Routes.RECORDING_UNASSIGNED`(引数なし)→ `RecordingScreen(clientId = -1L)`。要約後の `ResultScreen` で保存先を選ぶ: `MeetingViewModel.isClientAssigned()`(= `clientId >= 0`)が false のとき、タイトル入力の下に「既存から選ぶ/新規登録」の `FilterChip` を出し、既存は `LabeledDropdownField`(`viewModel.clients`)、新規は名前入力+グループ選択(`viewModel.clientGroups`)。保存は `saveMeetingToClient` / `saveMeetingToNewClient`(共通処理 `persistMeeting(targetClientId, title)`。`saveMeetingToNewClient` は要約成功を確認してから `addClient` = 空クライアントの残留防止)。`ResultScreen.onSaved` は `(clientId: Long) -> Unit`、直接録音フローでは `Navigation.kt` が `popBackStack(Routes.MAIN)` してから `clientDetail(savedClientId)` を開く。`RecordingDraftStore` の下書きは `clientId = -1` でも保存され、ホームの復元カードからも `RECORDING_UNASSIGNED` へ復帰できる。「要フォロー」「予定表」の全件ページ(旧 `Routes.FOLLOWUP_LIST`/`SCHEDULE`)は下部ナビの ToDo/予定表タブに昇格
- **設定画面 新設(2026-09-06、`ui/settings/`)**: `Routes.SETTINGS`、ホーム画面ヘッダーのヘルプアイコンの左に歯車アイコンから遷移。内容は (1) 打ち合わせのリマインドON/OFF(旧 `NotificationScreen` から移設。`SettingsViewModel` が `ReminderPrefs`/`ReminderScheduler`/`POST_NOTIFICATIONS` 権限リクエストを保持。`NotificationViewModel` からは同ロジックを削除し履歴+予定リストのみに縮小) (2) 背景(テーマ) (3) **データのバックアップ(2026-09-09)**。`NotificationScreen`/`ClientListScreen`/`ScheduleScreen` のヘッダーは `ClientDetailScreen`(アーカイブ画面)と同じ `TopAppBarDefaults.topAppBarColors(containerColor = primaryContainer, ...)` 配色に統一し、タイトル左にホーム画面の対応タイルと同じアイコン(People/CalendarMonth/Notifications)を表示
  - **クライアント CSV 入出力(2026-09-09、`data/backup/ClientCsv.kt`)**: 純粋関数の RFC4180 パーサ + `MeetingRepository.exportClientsCsv`/`importClientsCsv`(`CsvImportResult(added, skipped)`)。列 = 名前/グループ/メール/電話/流入経路/紹介元/備考。取り込みはヘッダー名でマッピング(順不同・列欠落OK)、**名前が空の行は無視・同名の既存クライアントはスキップ(上書き/削除なし)**、グループは既存名に一致すれば割り当て(自動作成なし)。設定画面「データのバックアップ・移行」に2行、BOM 付き UTF-8 出力。`ClientCsvTest` あり。
  - **全データのバックアップ/復元(`data/backup/BackupManager.kt`)**: `sqlite_master` から全ユーザーテーブルを列挙 → `SELECT *` を JSON にシリアライズ(個別 Entity/DAO 非依存・スキーマ追従)。`format` / `dbVersion` / `exportedAt` / `tables` を持つ封筒。**パスワード任意**: 設定時は PBKDF2(SHA-256, 12万回) + AES-GCM で本体を包んだ別封筒(`encrypted:true`)。**復元 = 全置換え**(全テーブル DELETE → INSERT、FK 一時 OFF・トランザクション)。`backup.dbVersion > 現在` は拒否、`<` は不足カラムを DB のデフォルトで補完。復元成功後は `BackupManager.restartApp` で `Runtime.exit(0)` → 再起動(Room キャッシュ破棄)。UI は `SettingsViewModel.exportBackup/importBackup`(`BackupState`)+ `SettingsScreen` の SAF ランチャ(`CreateDocument`/`OpenDocument`、MIME `application/json`)+ パスワードダイアログ + 復元前の警告ダイアログ。ファイル名 `商談メモ-backup-<yyyy-MM-dd>.json`。**JVM テスト無し**(android.util.Base64 / Cipher 依存)
- **予定は独立エンティティ `schedules`(2026-09-09、DB v17)**: 旧来は「予定 = `MeetingEntity.nextMeetingDate`」で1クライアント1件・上書きだったが、`ScheduleEntity`(`clientId` FK CASCADE / `sourceMeetingId: Long?` / `startAtMillis` / `hasTime` / `title` / `note` / `participants` / `phase`)に分離。1クライアントに複数持てる。
  - **`nextMeetingDate` との連動**: AI が要約から拾った「次回打ち合わせ」は `sourceMeetingId` 付きの `ScheduleEntity` として作られる。`MeetingRepository.saveMeeting` / `setNextMeeting`(→ `syncNextMeetingSchedule`)が upsert/削除。起動時に既存分を1回だけ `backfillSchedules`(`SharedPreferences("app_migrations")` の `schedules_backfilled` フラグ)。予定表で AI 由来の予定を編集/削除すると `meetings.nextMeetingDate` 側も追従する。
  - **予定表(`ScheduleScreen`)**: 月カレンダー(`MonthCalendar` = **`OutlinedCard`(1.5dp `outline` 枠)+ 曜日行下の `HorizontalDivider` + 各日セルに 0.5dp `outlineVariant` の格子線**(2026-09-10、枠を強調)、予定日 + ToDo 期限日はセル下寄せの点(2026-09-11、数字から離した)、**今日は左上角の赤い三角(`Canvas`、`error` 色。丸枠は廃止)**(2026-09-11))+ 一覧(日付タップで絞り込み。**未選択時は「本日の予定」→「これからの予定」の2枠**(2026-09-10、`todaySchedules`/`upcomingSchedules` に分割。各枠0件でも見出し+空状態を表示)。**枠見出しは `SectionRule`(「——— 本日の予定 ・ N件 ———」中央ラベルの罫線)**(2026-09-10)、選択時はその日=過去も表示)。**一覧はホームの「直近の予定」と同じ見た目に統一**(2026-09-11、`ScheduleListPanel` = `surfaceContainer` 角丸パネル + 共有 `UpcomingRow` + 区切り線。旧 `ScheduleRow` カード + ⋮ メニューは廃止。編集/削除は行タップ → `ScheduleDetailDialog` から)。**予定の追加はヘッダー右の `AppIcons.CalendarPlus`(カレンダー枠+＋の独自 `ImageVector`、`ui/common/AppIcons.kt`。`CreateActionBlue` tint)**(2026-09-10、旧 `Icons.Filled.Add`。FAB は先に廃止済み)、行の ⋮「編集」→ `ScheduleFormDialog`(クライアント`LabeledDropdownField`(全クライアント)/ 日時 `NextMeetingDateTimeDialog` / 内容・タイトル / 参加者 / **会議URL / 場所(v24)** / メモ / 進捗ラベル `LabeledDropdownField<DealPhase?>`)。⋮「削除」= `ConfirmDialog`(AI 由来なら nextMeetingDate も戻る旨を表示)、⋮「カレンダーに追加」= `CalendarIntent.add`(`location` 対応、description に参加者/URL/メモ)。`ScheduleRow` は **日付時間(右上にフェーズchip)→ 会社名 ・ 参加者 → 予定名** の順**(2026-09-10、旧: タイトル+chip 先頭)** + 📍場所 + URL(下線・タップで `ACTION_VIEW`)+ メモ。**行タップは公開 `ScheduleDetailDialog`(2026-09-11、旧: クライアント画面へ直行)**: 項目は**ラベル上/値下の縦積み `DetailField`**(幅の折り返し対策。値 null は `outline` 色で「なし」・全項目を常時表示、値は `bodyLarge`)。日時/クライアント(名前自体がクライアント画面へのリンク)/参加者/場所/会議URL(タップで `ACTION_VIEW`)/メモ。区切り線の下にコンパクトなリンク行(`DialogLinkRow`、`bodySmall`+16dp chevron)=「前回の会議（アーカイブ）を開く」(`ScheduleViewModel.latestMeetingByClient` / `HomeViewModel.latestMeetingByClient` = `observeLatestMeetingPerClient` → clientId→meetingId。商談ゼロなら非表示)「カレンダーアプリに追加」(`CalendarIntent.add` = `ACTION_INSERT`。Google に限らず端末のカレンダーアプリを開く)。`onEdit`/`onDelete` は nullable — **予定表からは編集/削除ボタンあり、ホームの「直近の予定」(`UpcomingBoard`)からは閲覧のみ**(2026-09-11、`UpcomingBoard` の行タップを `onOpenClient` → `onOpenSchedule: (UpcomingItem)->Unit` に変更)。行の ⋮ メニューは従来どおり。`meetingUrl`/`location` は `ScheduleEntity`・`ScheduleWithClient`・`UpcomingItem` すべてに載る。**時刻ピッカー(`NextMeetingPickerDialog.kt` の時刻ダイアログ)は `TimePicker`(ホイール)と `TimeInput`(打ち込み)をタイトル右のアイコンで切替**(2026-09-10)。
  - **共通読み取り**: `ui/client/UpcomingBoard.kt` の `UpcomingItem`/`UpcomingRules.order(schedules, includePast)` + `ScheduleWithClient.toUpcomingItem()`。ホーム「直近の予定」(`HomeViewModel.upcoming`)・通知画面の予定リスト(`NotificationViewModel.upcoming`)・`MeetingReminderWorker`(`getSchedulesForReminder(from,to)`、`notification_log` キー `"sch-<id>-<date>"`、`meetingId=0`)すべて `repository.observeSchedules()` 由来。旧 `NextMeetingCandidate`/`getNextMeetingCandidates` は削除。
  - 通知画面: 予定行・履歴行のタップを**クライアント詳細**へ(`NotificationScreen(onOpenClient=)` 追加。履歴は `meetingId>0` なら商談詳細)。
- **要フォロー/次回の予定は0件でも常に表示**: `FollowupBoard`/`UpcomingBoard` から `if (items.isEmpty()) return` を撤去し、0件時は「フォローが必要な商談はありません」等の空状態メッセージを表示
- **クライアント追加・グループ移動はセレクトボックス**: `ClientListScreen` の「クライアントを追加」ダイアログはグループ選択(`ui/common/LabeledDropdownField.kt`、既定「未分類」)付きに変更(`MeetingRepository.addClient(name, groupId)`)。「グループに移動」もセレクトボックス+「変更」ボタンの確定式に変更(旧 RadioButton 一覧から置き換え)。`LabeledDropdownField` は総称型の再利用可能コンポーネントで、`ScheduleScreen` のクライアント選択でも使用

## コーディング上のルール

- **Composeのスコープメンバー関数を明示的にimportしない**: `Modifier.weight()`(RowScope/ColumnScope)、`ExposedDropdownMenu`(ExposedDropdownMenuBoxScope)、`Button`のcontentラムダ内の暗黙Row配置などは、明示的に`import`すると内部APIの同名シンボルと衝突してコンパイルエラーになる。`Row{}`/`Column{}`/`ExposedDropdownMenuBox{}`のブロック内で暗黙レシーバのまま使う
- `Modifier.menuAnchor(MenuAnchorType...)`は非推奨。`ExposedDropdownMenuAnchorType`を使う
- AGP 9.1.0はKotlinサポートを内蔵しているため、`org.jetbrains.kotlin.android`プラグインは併用しない(`kotlin.plugin.compose`と`kotlin.plugin.serialization`のみ適用)
- Compose BOM 2026.08系を使う場合は`compileSdk = 37`が必須(35/36だとAAR metadataエラー)
- 危険な権限(RECORD_AUDIO等)を使う新機能を追加する場合、マニフェスト宣言だけでなく**実行時パーミッションリクエストの実装まで必ずセットで**確認する(過去に実装漏れがあった、後述)

## テスト方法

```bash
./gradlew.bat testDebugUnitTest          # JVMユニットテスト(エミュレータ不要)
./gradlew.bat connectedDebugAndroidTest  # インストルメンテーションテスト(実機/エミュレータ必須)
```

- **サンプルデータ投入(デバッグビルド限定)**: 設定画面 → 「デバッグ」カード →「サンプルデータを投入」で、既存データを全消去して過去1年ぶんのクライアント16 / 商談52 / 案件18(成約6・失注4・進行中8、JPY+USD、一部は停滞)/ ToDo / 予定 / メールテンプレを投入(投入後に自動再起動)。`data/debug/SampleDataSeeder.kt`(DAO 直挿し・`Random(42)` で決定的・本番フロー非経由)、`SettingsViewModel.seedSampleData()`、UI は `SettingsScreen` の `if (BuildConfig.DEBUG)` ブロック。分析グラフ・パイプライン・停滞案件・カレンダー等をまとめて確認したいとき用。

エミュレータでの手動確認が必要な場合、`adb`はフルパス指定が必要(PATHに無いことがある):
```
/c/Users/zhong/AppData/Local/Android/Sdk/platform-tools/adb.exe
```

### adb操作の既知の落とし穴(Windows/Git Bash環境)

- `adb shell input keyevent 111`(ESCAPE)はGboardの「元に戻す」に割り当てられており、直前に入力したテキストを消す。キーボードを閉じたいだけなら入力欄以外をタップする
- `adb shell input tap`で座標指定する際、スクリーンショットの表示解像度と実機解像度の換算を手計算するとズレやすい。`adb shell uiautomator dump`→該当要素の`bounds`から中心座標を計算する方が確実
- Room DBの実ファイル名は`meeting-notes.db`(ハイフン、アンダースコアではない)
- DBを直接編集する場合: `adb exec-out run-as <pkg> cat //data/data/<pkg>/databases/<name>`(`//`二重スラッシュでWindows Git BashのMSYSパス変換を抑止、`exec-out`必須)で`.db`/`.db-wal`/`.db-shm`を取得し、ローカルの`.db-shm`を削除してからPythonの`sqlite3`モジュールで開く(stale shmがWALを空と誤認させることがある)。編集後は`PRAGMA wal_checkpoint(TRUNCATE); PRAGMA journal_mode=DELETE;`で単一ファイルに統合してから書き戻す
- 日本語テキストを`adb shell input text`で送る場合、IME予測変換の影響で文字化けすることがある(スクリーンショットで確認し、必要なら`%s`でスペースを区切るか単語ごとに送る)

## ビルド・実行方法

```bash
./gradlew.bat assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
adb shell am start -n com.manaapps.meetingnotes/com.meetingnotes.MainActivity
```

**パッケージ名に注意**: ストアの識別子(`applicationId`)は `com.manaapps.meetingnotes`、コード上のパッケージ名(`namespace`)は `com.meetingnotes` で**両者が異なる**。`adb` でアプリを指定する時・`run-as` する時・DBパスは `com.manaapps.meetingnotes` を使う。Activityクラスの完全名は `com.meetingnotes.MainActivity`。DBの絶対パスは `/data/data/com.manaapps.meetingnotes/databases/meeting-notes.db`。

## 変更時に注意すべき箇所

- 不要なリファクタリングは行わない。既存のコードパターン(ViewModelのファクトリDSL、Repository経由のDAOアクセス、フォルダ/グループの共通パターン等)に合わせて実装する
- 既存機能への影響を最小限にする。特に`MeetingViewModel`(録音〜保存の中心)や`MeetingRepository`(全機能が経由する)を変更する際は、他画面への影響を必ず確認する
- DBスキーマを変更する場合はEntityにデフォルト値を持つフィールドとして追加すると、既存のテストコードの修正が不要になることが多い(実例: `endedAt: Long? = null`, `groupId: Long? = null`)
- テスト(ユニット+インストルメンテーション)は改修のたびに実行し、可能であればエミュレータでの実機確認も行う

## 現在の開発状況(2026-09-03時点)

MVP相当の機能は一通り実装済み。Google Play Console でのクローズドテストを回している段階。すべての変更はコミット・push 済み(`origin/main`、`github.com/nebu965039-gamecreate/MeetingNotes`、**Public**)。

現在 `versionCode = 10` / `versionName = "0.1.9"`(= クローズドテスト6回目。`feature/solo-crm` の 1人CRM 拡張・リモート会議モード・Tier 1 CRM をまとめて配信)。Play Console 提出の詳細な進捗・手順は `docs/` を参照(`play-console-checklist.md`、`play-data-safety.md`、`play-store-listing.md`、`release-notes.md` の「6回目」)。**7回目(未配信、2026-09-09〜12の変更一式)は `release-notes.md` の「7回目」に配信前チェックリスト込みでまとめてある**。回帰テストの一覧・実機必須/AI可否の分担は `docs/test-plan.md` を参照(機能追加のたびにこの一覧へ追記する)。

### 対応済み(Play Console クローズドテスト準備)

- リリース署名設定(`signingConfigs`、`keystore.properties` 方式、Play App Signing)。ユーザーがアップロード鍵 `upload-keystore.jks` を生成済み(ローカルのみ・未コミット)。`./gradlew.bat bundleRelease` で署名済み AAB が出る
- プライバシーポリシー(`docs/privacy-policy.md/html`)。GitHub Pages で公開: `https://nebu965039-gamecreate.github.io/MeetingNotes/privacy-policy.html`
- データセーフティ フォームの記入内容(`docs/play-data-safety.md`)
- ストア掲載テキスト(`docs/play-store-listing.md`)+ アイコン512×512 / フィーチャーグラフィック1024×500 / スクリーンショット4枚(`docs/store-assets/`)
- **アプリアイコン刷新**(AGPテンプレート→「ふきだし+メモ罫線」/ M3 baseline purple `#6750A4`。`ic_launcher_foreground/monochrome/background`)
- `applicationId` を `com.manaapps.meetingnotes` に確定(公開後変更不可)。namespace は `com.meetingnotes` のまま
- `allowBackup="false"` + `data_extraction_rules.xml`(クラウドバックアップ・端末間転送を除外)
- **使い勝手の改善**(2026-09-01〜02): 無料枠を月3→**月5回**(`CreditPolicy.MONTHLY_FREE_CREDITS`)。**「使い方・ヘルプ」画面**(`ui/help/HelpScreen.kt`、`Routes.HELP`、クライアント一覧の TopAppBar に ? アイコン)。`TranscriptPreprocessor` に隣接重複文の除去・記号/長音の正規化を追加(音声認識のセッション連結で重複しやすいため)。**1回の文字起こし上限 約20,000字**(≈60〜80分、`AnthropicClient.MAX_TRANSCRIPT_CHARS` と Worker の `MAX_TRANSCRIPT_CHARS` を揃える。超過時は要約前に明示エラー・クレジット消費なし)。Worker: system/tools を prompt cache 対象に、ユーザーメッセージ冒頭に「現在の日付」を付与(次回打ち合わせの年ズレ対策)
- **要約フローの調整**(2026-09-02): tool に `strict: true`(スキーマ厳密化。Worker が 400 なら自動で strict なしにフォールバック、`STRICT_TOOL` env で無効化も可)。`max_tokens` 1500→2000(長時間商談での切り詰め防止)。**インタースティシャル広告を要約完了後→要約リクエスト直後**に移動(待ち時間に表示、`InterstitialAdController` の頻度キャップ 60分→90秒)。**2026-09-07: `submitForSummary` は要約を `async` で走らせ `delay(1200)` 後にまだ未完了のときだけ `tryShow`**(短時間で終わる要約で「結果が出た後に広告」になるのを回避)。`ResultScreen` の要約中表示を段階メッセージ(「決定事項を抽出中...」等)に。真のトークンストリーミングは tool_use 構造化出力とは相性が悪く未実装(UX上の利点が小さいため)
- **広告収益の見直し**(2026-09-02): `TranscriptPreprocessor` に相槌のみの文の除去・つなぎ言葉の追加削減を追加(要約コスト削減)。`BannerAdView` に `BannerAdFormat`(アダプティブ帯 / 300x250 MediumRectangle)を追加し、**商談詳細画面に300x250バナーを新設**(次回打ち合わせセクションとエクスポートの間)、**商談アーカイブ画面の bottomBar にもアダプティブバナーを新設**(録音開始ボタンの上)。録音画面に経過時間表示 + 50分超で区切りを促す警告、編集画面に文字数カウンタ + 上限超過時は要約ボタンを無効化(上限に達する前に気づけるように)
- **録音の安全性**(2026-09-02): 録音の「停止」を誤タップ防止のため**スライド操作**に変更(`SlideToStop`、`Modifier.draggable`)。**下書き自動保存**(`data/RecordingDraftStore`、SharedPreferences。録音中は数秒おき + 編集中は都度保存、保存成功で消去)。プロセス終了しても文字起こしが残り、クライアント一覧の先頭の復元カードから再開できる。`RecordingScreen` に `BackHandler` を追加(**従来は録音中にシステムバックで戻ると認識器が止まらず孤立していたバグを修正**)。録音中の戻る操作は中止確認ダイアログを挟む
- **APIキープロキシ フェーズ1**(2026-09-01): `server/`(Cloudflare Worker、TS)。アプリは `AnthropicClient` から Worker へ `{transcript}` を POST するだけ。`ANTHROPIC_API_KEY` はアプリから削除、`SUMMARY_PROXY_URL` / `SUMMARY_PROXY_APP_TOKEN` を `local.properties` から読む。プロンプト/toolスキーマ/モデルは Worker 側に移動。**ユーザーがデプロイ済み**: `https://meetingnotes-summary-proxy.manaapps.workers.dev/summarize`(疎通・実要約とも確認済み)
- **APIキープロキシ フェーズ2 コード実装済み**(2026-09-01、未有効化): Worker `src/integrity.ts`(Play Integrity トークンを GCP サービスアカウント経由で `decodeIntegrityToken` 検証、`PLAY_INTEGRITY_ENABLED` = off/audit/enforce)、KV による日次上限(`DAILY_REQUEST_CAP`、`RL` バインディング任意)。アプリ `data/remote/IntegrityTokenProvider.kt`(Standard Integrity API、`PLAY_INTEGRITY_CLOUD_PROJECT_NUMBER` 未設定なら null 返却で無害)、`AnthropicClient` が `x-integrity-token` ヘッダを付与、`requestHash` = SHA-256(transcript) hex を app/Worker で一致させる。deps: `com.google.android.play:integrity:1.4.0` + `kotlinx-coroutines-play-services:1.9.0`。有効化手順は `server/README.md`「フェーズ2」
- **AdMob 本番ユニットID登録済み・切替はフラグ制御**(2026-08-31〜09-01): 本番アカウント `ca-app-pub-7474417689976149`。`build.gradle.kts` は `local.properties` の `ADMOB_USE_PRODUCTION_ADS`(既定 false)で本番/テストを切替。**クローズドテスト・ローカル開発はすべてテスト広告のまま**(Google公式テストID)= 無効トラフィックのリスクなし。本番/オープンテスト用の AAB をビルドするときだけ `ADMOB_USE_PRODUCTION_ADS=true` にする。App ID は `manifestPlaceholders["admobAppId"]`、ユニットIDは `BuildConfig.ADMOB_*_UNIT_ID`。本番切替後に実機テスターへテスト広告を出す用の `ADMOB_TEST_DEVICE_IDS`(カンマ区切り)→ `MeetingNotesApp` で `RequestConfiguration.setTestDeviceIds`

### 「1人CRM」機能拡張(2026-09-04〜、方向性 承認済み・`feature/solo-crm` ブランチで開発)

競合分析の結果、汎用議事録アプリとの差別化のため **「記録」から「一人商談のやりきり支援」へ** 軸を移す。基本設計・コスト試算・競合分析は Artifact「1人CRM 基本設計」(`https://claude.ai/code/artifact/be51e1cf-e7c3-4962-a2aa-e416f87a2605`)。決定事項:

- **無料**: F1 フォローボード / F2 前回のおさらい(ブリーフィング) / F3 商談フェーズ / F5 フォローアップ下書き生成
- **Pro**: F4 要約→文字起こしの根拠リンク / F6 ヒアリング分析。Pro 価格 **月¥980 / 年¥7,800**(Billing 実装時に確定)
- 音声保存は**しない**(プライバシー訴求維持)。フェーズは8段階(初回接触/ヒアリング/提案/見積提示/検討中/成約/保留/失注)
- 実装順: **P1 F1 → P2 F3(+v6 マイグレーション)→ P3 F2+F5 → P4 Billing+F4+F6**
- 無料機能(F1・F2・F3・F5)をまとめて次回クローズドテスト(v0.1.9 想定)へ
- **P1 完了**(`6a45e97`): `MeetingDao.observeLatestMeetingPerClient`(射影 `ClientLatestMeeting`)、`FollowupRules`(純粋関数)、`FollowupBoard` composable、`ClientListViewModel.followups`。閾値 14日
- **F1 = ToDo(要フォロー)**: `MeetingEntity.followedUpAt`(**DB version 9 + `MIGRATION_8_9`**)= 「完了」時刻。`FollowupRules.compute`(純粋関数)が **ToDo** を判定: 各クライアントの最新商談で `followedUpAt == null`(=「完了」を押したら**二度と出ない**) & 成約/失注でない & 最終商談から `RECENT_WINDOW_DAYS`(30日)以内、のみ。**次回予定の有無・経過日数は判定に使わない**(打ち合わせが決まっていてもお礼メール等の連絡は別途必要、という考え方)。ソートは録音が新しい順。`FollowupItem` = `client`/`meetingId`/`lastRecordedAt`/`phase`。行タップは `meetingId` で商談詳細へ、各行に「完了」ボタン(`markMeetingFollowedUp`)。
  - **旧仕様の破棄(2026-09-08)**: `FollowupReason`(NEEDS_EMAIL/STALE)・`FollowupItem.daysSince`/`reason`・「フォロー済みでも14日放置で再表示(STALE)」・`hasUpcomingMeeting` チェック・`THRESHOLD_DAYS` は**廃止**(「完了したのに再び出る」混乱と初回導入時の大量表示への対策)。
  - **`FollowupListScreen`(下部ナビ ToDo 画面)は「個別 ToDo」粒度に刷新(2026-09-10、DB v28)**: 旧「クライアント単位のカード + クライアント単位スヌーズ」を廃止し、**クライアント詳細の ToDo タブと同じ行**(共有 `ui/common/TodoRow.kt`)にした。3タブ(`FolderTabRow`): 「ToDo」= 全クライアント横断の未完了 ToDo(期限順・未設定は後、スヌーズ中は最後尾に残し `😴 M/d まで` チップ)/「スヌーズ」= スヌーズ中の未完了だけ(チェックボックス無し・「解除」だけ)/「完了」= 完了済み個別 ToDo(id 降順、チェックを外すと復活)。`FollowupListViewModel` は `observeAllTodos()` + `observeClients()` を combine(`TodoScreenItem`)。行タップは商談由来 → 商談詳細(`onOpenMeeting`)、手動 → クライアント詳細。**スヌーズは ToDo 一件ごと**(`ui/common/SnoozeMenu.kt`: 1日/3日/1週間/1ヶ月/日付指定、`todos.snoozedUntil`)。スヌーズ中の ToDo は **下部ナビのバッジ / ホーム「やること(期限あり)」`DueTodoBoard` / ホームの ToDo ボード `FollowupBoard` / 期限リマインド `MeetingReminderWorker` からは外れる**(該当 DAO クエリに `(snoozedUntil IS NULL OR snoozedUntil <= :nowMillis)` を追加、repo が `System.currentTimeMillis()` を渡す)。**旧 `clients.followBoardSnoozedUntil`(v23)・`FollowupRules.computeSnoozed`・`setClientFollowSnooze` は未使用化**(列は残置)。ホームの `FollowupBoard`(クライアントカード)は `FollowupRules.compute` のまま(スヌーズ済みだけのクライアントは件数0になり自然に消える。※ `FollowupBoard` 自体は2026-09-12にホームから廃止済み、上記「ホーム画面」項参照)。
  - **ToDo タブの追加機能(2026-09-12)**: 「ToDo」サブタブにフィルタチップ行(`TodoDueFilter`: すべて/期限が3日以内/期限切れ、`FollowupListViewModel.dueFilter`/`setDueFilter`)を追加。`openTodos` はこのフィルタで絞り込むが、フォルダタブの件数バッジ(`activeTodoCount`)はフィルタの影響を受けない「今やるべき総数」を維持(下部ナビのバッジと一致させるため、フィルタ前の `openTodosAll` から算出)。「完了」サブタブに**「すべて削除」**(`DeleteSweep` アイコン + `ConfirmDialog` + `MeetingRepository.deleteAllDoneTodos()` → `TodoDao.deleteAllDone()`、全クライアント横断で完了済みを一括削除)。「スヌーズ」サブタブの行は、サブ行が元の期限表示と紛らわしいため `TodoRowData.dueDate`/`deadlineText` を渡さず**スヌーズマーク(😴)+日時のみ**に簡略化(`TodoRow.kt` の `SnoozeChip` 表示、元の期限情報は表示しない)。**フィルタチップの選択色(2026-09-13)**: M3 既定の `FilterChip` 選択色(`secondaryContainer`、紫)がテーマと合っていなかったため、`FilterChipDefaults.filterChipColors(selectedContainerColor = primary, selectedLabelColor = onPrimary)` でネイビーに統一。
  - **日付だけを選ぶダイアログ `ui/common/NextMeetingPickerDialog.kt#PickDateDialog`(2026-09-10)**: `DatePickerDialog`(`showModeToggle` でカレンダー/打ち込み切替)。スヌーズの「日付を指定」等、「次回打ち合わせ」の枠を出したくない場面用。
  - **フォルダ型タブ `ui/common/FolderTabRow.kt`(2026-09-10、`FollowupListScreen`(ToDo画面)と `AnalyticsScreen`(分析)で採用)**: ストリップ地・非選択タブ = **ヘッダーと同じ `MaterialTheme.colorScheme.surface`**、選択中タブ + 直下の一覧 = **`FolderTabDefaults.sheetColor`(淡いティール B1、light `#E3F0EE` / dark `#17302D`。実効ダークは `surface.luminance() < 0.5` で判定 = 手動テーマ切替に追従)**。選択タブが「色の付いた1枚のシート」として手前に出る(高さ44dp・`shadowElevation` 1dp)、非選択は高さ34dpでヘッダーに沈む。**採用画面はコンテンツ領域に `.background(FolderTabDefaults.sheetColor)` を明示**。件数は `PrimaryTabRow` の "(N)" 表記をやめ、ラベル横の小ピル(`FolderTab(label, count)`、count>0 のみ)。他画面は従来どおり `PrimaryTabRow`。
- **商談の管理番号(No.)**: `MeetingDetailScreen` のヘッダーに `No. yyyyMMddHHmm`(録音開始 `recordedAt` から都度生成、DB 保存なし。同一分の録音は同番号)。`meetingNo()` は `MeetingDetailScreen.kt` 内の private 関数
- **メール文面テンプレート(2026-09-09、DB v26)**: `email_templates`(id/name/body/createdAt)+ `EmailTemplateDao`。`MeetingRepository.observeEmailTemplates`/`addEmailTemplate`/`updateEmailTemplate`(`updateContent` で createdAt 保持)/`deleteEmailTemplate`。管理は `Routes.EMAIL_TEMPLATES`(`ui/settings/EmailTemplatesScreen`+`EmailTemplatesViewModel`)、設定画面から遷移。本文の `{クライアント名}`/`{日付}`/`{次回打ち合わせ}` は `util/TemplateVars.apply` で使用時に差し込み(未解決の `{...}` は残さない、JVM テストあり)。`MeetingDetailScreen` の `FollowupDialog` 上部に `FilterChip` 一覧を出し、選択すると差し込み済み本文 + コピー/共有(AI 下書きとは独立)。
- **フォローアップ下書き(2026-09-07)**: 基本は要約時に一緒に生成。`/summarize` の tool スキーマに `followupDraft`(丁寧体1本、ToDo を「次のアクションのお願い」に織り込む)を追加。ただし **`required` には入れない**(長い商談で切り詰められて欠落しても要約全体を失敗させない。アプリは null 許容)。`MeetingSummary.followupDraft` / `SummaryDto` / `toDomain` / `saveMeeting` 対応、**`MeetingEntity.followupDraft`(DB version 10 + `MIGRATION_9_10`、`10.json`、`MigrationTest` 9→10)**。付いていない商談(旧データ / Worker 未更新)は商談詳細で **1回だけ** 後追い生成できる: Worker `POST /followup`(`{summary}` = 要約のプレーンテキスト、`FOLLOWUP_SYSTEM`、`generateText` 経由)、`AnthropicClient.generateFollowup`、`MeetingDetailViewModel.generateFollowup` → `repository.setMeetingFollowupDraft`(`MeetingDao.updateFollowupDraft`)で保存。以後は再生成不可(`FollowupState`: `Idle`/`Loading`/`Ready`/`Empty`/`Error`。トーン切替なし)。Worker `MAX_TOKENS` 2000→2400。**Worker 再デプロイ必要**
- **フェーズタグの見た目統一(2026-09-07)**: `DealPhaseChip` は「枠あり + 塗りつぶし」の1スタイルに固定(`RoundedCornerShape(6dp)`、`labelMedium`)。予定(`UpcomingBoard`/`ScheduleScreen`)・商談詳細・アーカイブ・フォロー一覧すべて同じ見た目。一時追加した `outlined` パラメータは撤去。**配色は `ui/theme/PhaseTagColors.kt`**: アプリ全体が M3 紫系で埋もれるため、各フェーズに別系統色(初回接触=スレート/ヒアリング=琥珀/提案=青/見積提示=ティール/検討中=オレンジ/成約=緑/保留=温グレー/失注=ローズ)を割り当て。色相は不変でライト/ダークの明度を入れ替え(`of(phase, darkTheme)`)。`DealPhaseChip` は `MeetingNotesApp.themeModeState`(LIGHT/DARK/SYSTEM)から実効ダークを判定
- **アーカイブ `MeetingRow` の見た目(2026-09-07)**: タイトルは太字(`titleMedium` + `SemiBold` + `onSecondaryContainer`)で、右端に `DealPhaseChip`(タップで変更)。※白帯背景を一度試したが視認性が悪く撤回。要約プレビューは `conciseSummary()`(先頭1文・最長60字)で見切れ回避
- **P2 完了**: `DealPhase`(enum、`data/model`)、`MeetingEntity` に `dealPhase`/`phaseOverride` 列、**DB version 6 + `MIGRATION_5_6`**(`app/schemas/.../6.json` コミット、`MigrationTest` androidTest 追加・通過)。Worker の tool スキーマに `dealPhase` enum + SYSTEM_PROMPT ルール9。`SummaryDto`/`toDomain`/`MeetingSummary`/`saveMeeting` 対応。`ui/common/DealPhaseChip.kt`(`DealPhaseChip` + `DealPhasePickerDialog` + `MeetingEntity.effectivePhase()`)。アーカイブの `MeetingRow` と `MeetingDetailScreen` にチップ(タップで変更)。F1 のフォロー理由にフェーズ表示、WON/LOST は除外
- **P3 完了**: (F5)Worker `POST /followup`(tool なしのプレーン補完、共通ヘルパー `guard`/`generateText`)、`AnthropicClient.generateFollowup`、`MeetingDetailViewModel.FollowupState`、商談詳細に「フォローアップの下書きを作る」+ `FollowupDialog`(丁寧/カジュアル、コピー/共有)。(F2)Worker `POST /briefing`、`AnthropicClient.generateBriefing`、`client_briefing` テーブル + `ClientBriefingDao` + **DB version 7 + `MIGRATION_6_7`**(`7.json` コミット、`MigrationTest` に 5→7 チェーン追加・通過)。`ui/briefing/`(`BriefingScreen` + `BriefingViewModel`)、`Routes.BRIEFING`。2回目以降の録音は「録音開始」→ 前回のおさらい画面 →「録音を始める」。`ClientDetailScreen` の ⋮ に「前回のおさらい」。おさらいは商談が増えたら再生成(`sourceMeetingCount`)
- **F7 予定カレンダー + リマインド 完了**(P3 に同梱): `data/model/NextMeetingTime`(ISO 日付/日時の解釈)、`MeetingDao.getNextMeetingCandidates`、`updateNextMeeting`、`observeLatestMeetingPerClient` 射影に `meetingId` 追加。`notification_log` テーブル + `NotificationLogDao` + **DB version 8 + `MIGRATION_7_8`**(`8.json` コミット、`MigrationTest` に 7→8 追加)。`notifications/`(`NotificationHelper` チャンネル `meeting_reminders`、`ReminderPrefs` 既定ON、`MeetingReminderWorker` = WorkManager 12h 周期、当日/前日で未発火のものを通知+ログ、`ReminderScheduler` を `MeetingNotesApp.onCreate` で登録)。`POST_NOTIFICATIONS` 権限。`ui/client/UpcomingBoard`(クライアント一覧の「近日の予定」カード)、`ClientListViewModel.upcoming`。商談詳細の「次回打ち合わせ」に日程の手動設定(`NextMeetingDatePickerDialog`)+「カレンダーに追加」(`util/CalendarIntent`、`ACTION_INSERT` インテント・権限不要)。クライアント一覧 TopAppBar のベルアイコン → `ui/notifications/NotificationScreen`(リマインドのON/OFF+権限リクエスト、予定リスト、通知履歴)、`Routes.NOTIFICATIONS`
- **Worker デプロイ済み(2026-09-06)**: dealPhase スキーマ(P2)+ `/briefing` `/followup`(P3)を `wrangler deploy` 済み。以降 `server/src/index.ts` を変更したら再デプロイが必要
- **ホーム画面への集約 完了**(2026-09-05): 上記「アーキテクチャ・設計上の重要事項」の「ホーム画面」項を参照。F1(フォローボード)と F7(予定カレンダー)の UI を `ClientListScreen` から独立した `ui/home/HomeScreen.kt` に統合し、「進行中のフェーズ」ダッシュボード(F3 の集計)を新設。**この変更により P1/F7 完了時点の記述にある `ClientListViewModel.followups`/`.upcoming`、クライアント一覧 TopAppBar のベルアイコン、「クライアント一覧の近日の予定カード」は廃止・移設済み**(履歴として上記は残すが現状と一致しない)。デザイン検討は Artifact「商談メモ ホーム画面デザイン」(`https://claude.ai/code/artifact/f8c9c49e-5532-452c-a2bd-5e97d1ba8752`)
- **リモート会議モード Phase 1(2026-09-08)**: PC/Web会議で相手の声を拾えない問題への対応。録音開始時に `RecordingModePicker` で「対面」/「リモート会議」を選ぶ(`MeetingType` enum、`data/model`)。
  - **対面**: 従来の `TranscriptionManager`(オンデバイス、音声は端末外に出ない)。
  - **リモート会議**: `speech/AudioFileRecorder`(`MediaRecorder` → Opus/Ogg 16kHz 24kbps mono、`AudioSource` は UNPROCESSED→CAMCORDER→MIC)でファイル録音(録音中のライブ文字起こしは無し、音声レベルのみ)。停止 → `RecordingPhase.Transcribing` → `AnthropicClient.transcribeAudio` で Worker `POST /transcribe` へバイナリ送信 → **Cloudflare Workers AI Whisper**(`@cf/openai/whisper-large-v3-turbo`、`wrangler.toml` の `[ai] binding = "AI"`)→ 文字起こし結果を既存の編集フローへ。失敗時は同じファイルで再試行(`retryTranscription`)。
  - **課金**: `ProAccess.isPro` で分岐。Pro = 月40回(`CreditPolicy.ONLINE_TRANSCRIPTION_PRO_MONTHLY`)、無料 = 月1回 + リワード広告で最大+4回(`user_credits.onlineTranscriptionsUsed/Bonus`、月次リセットは `getOrInitCredits` に同梱)。`MeetingRepository.remainingOnlineTranscriptions/consumeOnlineTranscription/grantOnlineTranscriptionBonus`。消費は文字起こし**成功時**。
  - **同意**: 初回リモート選択時に `RemoteConsentPrefs`(SharedPreferences)で「音声をサーバー送信」の同意ダイアログ。
  - `MeetingEntity.meetingType` に記録し、商談詳細のヘッダーとエクスポート(`形式: 対面/リモート会議`)に表示。`RecordingDraftStore.Draft.meetingType` も保存。**DB version 11 + `MIGRATION_10_11`**(`11.json`、`MigrationTest` 10→11)。
  - 音声上限: `AudioFileRecorder.MAX_DURATION_MS`(45分)で自動停止、Worker `MAX_AUDIO_BYTES`(12MB)。**長時間のチャンク分割は Phase 2**。
  - **Worker 再デプロイ必要**(`[ai]` バインディング追加 + `/transcribe`)。プライバシーポリシー / データセーフティの更新は Phase 2(未対応)。コスト試算: Whisper $0.0005/音声分(40分商談で ~¥3)。

- **Tier 1 CRM 補強(2026-09-08)**: 他AIの機能提案(`docs` 外)との差分埋め。**DB version 12 + `MIGRATION_11_12`**(`12.json`、`MigrationTest` 11→12)。
  - **ToDo 期限の日付解決**: Worker の tool スキーマに `todos[].deadlineDate`(ISO or null)を追加。アプリは `deadlineDate ?? TodoDueDate.parse(deadline)` を `TodoEntity.dueDate` に保存(`data/model/TodoDueDate` = "金曜"/"9/15"/"来週月曜" 等をパース、JVM テストあり)。**Worker 再デプロイ必要**
  - **ホーム「期限切れ」「3日以内のToDo」カード**(`ui/home/DueTodoBoard`。2026-09-12、旧称「やること(期限あり)」から改称のうえ2枚に分割): 期限切れ + 今日 + 3日以内の未完了 ToDo。`TodoDao.observeOpenTodosWithDueDate` → `OpenTodo` 射影。各カード最大5件・「すべて表示」でToDoタブへ絞り込み付き遷移。チェックで完了、タップで商談詳細
  - **ToDo 期限リマインダー**: `MeetingReminderWorker` が本日期限の未完了 ToDo も通知(`notification_log` の `scheduledFor = "todo-<id>-<date>"` で重複防止)
  - **予定表**: カレンダーの丸印に ToDo 期限日を追加。日付選択時に「この日が期限のToDo」一覧
  - **クライアント詳細**: ToDo タブ(ToDo/完了の2サブタブ)。※旧「アーカイブの上の未完了ToDoセクション」から 2026-09-09 にタブ化
  - **手動 ToDo 追加(2026-09-09、DB v19)**: ToDo サブタブ上部の「ToDoを追加」から `TodoFormDialog`(内容 + 任意の期限日、`NextMeetingDateTimeDialog` 再利用)。`meetingId = null`・`clientId` セットで `todos` に入る(`MeetingRepository.addManualTodo`/`updateTodoContent`/`deleteTodo`、`ClientDetailViewModel.addManualTodo`/`updateTodo`/`deleteTodo`)。行タップ: 商談由来(`meetingId != null`)→ 商談詳細 / 手動 → 編集ダイアログ(削除ボタン付き)。要約由来・手動とも `observeOpenTodoCountByClient`/`observeOpenTodosWithDueDate`/リマインダー/ホームの各ボードに並ぶ。`OpenTodo.meetingId` は nullable(手動は `DueTodoBoard` タップでクライアント詳細へ、`MeetingReminderWorker` は `meetingId ?: 0L`)。`MeetingArchiveSearch` へ渡す ToDo は `meetingId != null` のみ。
  - **商談詳細**: `TodoRow` に解決済み期限(M/d)を併記
  - **ホーム/全件ページの「ToDo」ボード**(`FollowupBoard`/`FollowupListScreen`): 行タップを**クライアント詳細**へ変更(旧: 商談詳細)。クライアント名の右に `TodoCountBadge`(未完了 ToDo 件数)。件数は `TodoDao.observeOpenTodoCountByClient`(`todos` 直接, `isDone=0`, `GROUP BY t.clientId`。v19 以降 `meetings` JOIN 不要)→ `MeetingRepository.observeOpenTodoCountByClient(): Flow<Map<Long,Int>>` → `FollowupRules.compute(…, openTodoCountByClient=)`。`FollowupBoard` の「ToDo」= 各クライアント最新商談で `followedUpAt==null` かつ 成約/失注でない かつ 最終商談から30日以内(`DueTodoBoard` の「やること(期限あり)」は個々の期限付きタスク単位で別物)
- **フォローアップメール ToDo の自動起票(2026-09-08、DB v15)**: `MeetingRepository.saveMeeting` が要約成功時に、要約由来の ToDo に加えて **「お礼・フォローアップのメールを送る」ToDo を必ず1件**起票する(`TodoEntity.isFollowupEmail = true`、`dueDate` = 録音日の翌日、`assignee` = 「自分」。フェーズ問わず・既存商談へのさかのぼりはしない)。これでクライアントの ToDo リスト/`TodoCountBadge`/ホーム「期限切れ・3日以内のToDo」/期限リマインダーに要約 ToDo と並んで出る。
  - **(旧記述、2026-09-12にホームの当該ボードごと廃止)ホーム「ToDo」ボードは ToDo 駆動**(2026-09-08 改): `FollowupRules` は「未完了 ToDo が1件以上あるクライアント」を出すだけ。行内「完了」ボタンは廃止(ホーム `FollowupBoard`・全件 `FollowupListScreen` の ToDo タブ両方)。クライアント画面/商談詳細で ToDo を全部チェックするとボードから自動的に消える。ヘッダー件数はクライアント数でなく未完了 ToDo 合計。上記「ホーム画面」項の「ToDo」ボード廃止を参照。
  - **定期フォローのスヌーズ(2026-09-09、DB v23)**: `clients.followBoardSnoozedUntil`(epoch millis?)。`FollowupRules.compute` に `nowMillis` 引数を追加し、`followBoardSnoozedUntil > now` のクライアントを ToDo ボードから除外(ホーム `FollowupBoard`・全件 `FollowupListScreen` 両方)。`FollowupRules.computeSnoozed` = 現在スヌーズ中のクライアント(再表示が近い順)。`FollowupListScreen` は **3タブ(ToDo / スヌーズ / 完了)**、ToDo 行の `trailing` に `SnoozeMenu`(1週間/2週間/1ヶ月/3ヶ月)、スヌーズタブに「解除」。`MeetingRepository.setClientFollowSnooze(clientId, until)` / `FollowupListViewModel.snooze(clientId, days)` / `unsnooze`。`FollowupItem.snoozedUntil` はスヌーズ一覧でのみ非 null。
  - **`followedUpAt` との同期(レガシー互換)**: `isFollowupEmail` の ToDo を完了/未完了にすると `MeetingRepository.setTodoDone`(`todoDao.getById` で判定)が `meetings.followedUpAt` を更新する。`FollowupListScreen` の「完了」タブ(`observeFollowedUpMeetings` = `followedUpAt IS NOT NULL`)はこの同期で埋まり、「ToDoに戻す」(`clearMeetingFollowedUp` → `todoDao.followupEmailTodoId` 経由でメール ToDo も未完了へ)で戻せる。旧データ(メール ToDo 無し)は要約由来 ToDo が全部完了すればボードから消える。
  - **クライアント画面「未完了のToDo」→「ToDoリスト」に改称**、行頭は `Checkbox`(旧 `CheckCircleOutline` IconButton)。
- **クライアント情報 閲覧/編集の分離(2026-09-08、2026-09-09 に閲覧側をタブ化)**: 閲覧 = `ClientInfoContent`(`ClientInfoScreen.kt`) = `ClientDetailScreen` の**情報タブ**。クライアント名/グループ/現在のステータス(=最新商談の実効フェーズ、`DealPhaseChip`)/メール/電話/担当者一覧/備考。「情報を編集」ボタン → **`ClientEditScreen`(`Routes.CLIENT_EDIT`)**: 名前/グループ/メール/電話/備考 + 担当者の追加・編集・削除。両方 `ClientInfoViewModel`。担当者は **`client_contacts` テーブル(氏名/役職メモ/メール/電話。DB v13 `MIGRATION_12_13` + v14 `MIGRATION_13_14`、`14.json`、`MigrationTest` 12→13/13→14)** + `ClientContactDao`。会社レベルの `clients.email/phone` とは別に**担当者ごとにメール・電話を持てる**。`updateClientInfo` に `memo` あり。旧スタンドアロン `ClientInfoScreen` + `Routes.CLIENT_INFO` は廃止(2026-09-09)、`ClientDetailScreen` の引数は `onOpenClientInfo` → `onEditClient`
- 詳細な機能対照(実装済み/一部/未実装/不要/将来)と「案件レイヤー(Tier 2)」の検討は別途

### 未完了のタスク(優先度順)

0. **リモート会議モード Phase 2**: ~~(a) プライバシーポリシー(`docs/privacy-policy.*`)とデータセーフティ(`docs/play-data-safety.md`)に「リモート会議モードでは音声を文字起こしのため一時的に処理・保存しない」を追記~~ **完了(2026-09-08)**: `privacy-policy.md/html` の 2.1 を対面/リモート会議の2モードに分割 + データ保存表 + 権限表(`POST_NOTIFICATIONS` 追記)、`play-data-safety.md` の音声データ申告を「収集=はい/共有=はい/処理は一時的=はい」に変更(D.4 に Cloudflare 規約の最終確認メモ)、`play-store-listing.md` のプライバシー文言も修正。 (b) 長時間録音のチャンク分割(45分上限の緩和) (c) `ProAccess.isPro` を実購入判定に(Billing 実装後) (d) Whisper の実コスト・レイテンシ・日本語精度の実測。**Worker の `[ai]` バインディングは Cloudflare 側で Workers AI の有効化が必要な場合あり**
1. **APIキープロキシ フェーズ2 の有効化**(製品版公開前): コードは実装済み(`server/` + アプリ)。ユーザーが GCP/Play Console 設定を行って有効化する段階。(a) KV による全体日次上限 (b) Cloudflare ダッシュボードの IP レート制限 (c) **Play Integrity**(app: `IntegrityTokenProvider`、Worker: `src/integrity.ts`、`PLAY_INTEGRITY_ENABLED` で off/audit/enforce)。手順は `server/README.md` の「フェーズ2」。(d) 端末ごとのクレジット管理をサーバー側へ、は AdMob SSV・プライバシーポリシー更新を伴うため Billing と合わせて別タスク
2. **Google Play Billing — 基盤は実装済み(2026-09-09)**、残りは Play Console 設定 + gating 有効化。`billing/BillingManager.kt`(`com.android.billingclient:billing` 7.1.1、`PurchasesUpdatedListener`)= 接続 / `queryProductDetailsAsync`(商品 `meetingnotes_pro`・SUBS) / `launchPurchase(activity)` / `queryPurchasesAsync` + `acknowledgePurchase`(**クライアント側検証のみ**、サーバー検証は将来)。`MeetingNotesApp.billingManager`(lazy)を `onCreate` で `start()`、`isPro` フローを `ProAccess.setPro` に接続。`MainActivity.onResume` で `refreshPurchases()`。`ProAccess` は `MutableStateFlow` ベースに変更(`isProFlow` / `isPro` / `gatingEnabled` = `BuildConfig.PRO_GATING_ENABLED` / `shouldLock`)。`ProPaywallDialog` は価格(`monthlyPriceLabel()`)+「登録する」で購入フロー起動(`LocalActivity`)。設定画面に「Pro(サブスクリプション)」行(未登録→ペイウォール / 加入済→Play のサブスク管理)。**`PRO_GATING_ENABLED` は既定 false**(local.properties で切替、AdMob と同方式)= ロックは一切出ない。**残タスク**: (a) Play Console でサブスク商品 `meetingnotes_pro` + 月額ベースプラン登録 (b) ライセンステスト確認 (c) `PRO_GATING_ENABLED=true` (d) 各 Pro ゲート(売上ビュー実装済 / エクスポート形式 4b / 透かし 4 / PDFパスワード 4c / F4 / F6 / リモート会議 `ProAccess.isPro` Phase 2c)の解禁 (e) 年額プラン (f) サーバー検証。**年額は未実装**(まず月額のみ)。
3. 不正リセット対策フェーズ2(端末ごとのクレジット管理をサーバー側へ)。上記プロキシ フェーズ2に統合
4. 透かしのON/OFFをサブスク状態で自動判定(Billing 実装後 — 基盤済、gating 有効化と `ProGate` 配線が残り)
4b. **エクスポート形式のサブスク制限**(Billing 実装後): 形式は実装済み(PDF/Word/Markdown=議事録全体、Excel/CSV=ToDoのみ、.ics=次回打ち合わせ)。**ロック表示の仕組みは実装済み**: `billing/ProAccess`(`gatingEnabled` = `BuildConfig.PRO_GATING_ENABLED` 既定 false / `isPro` は `BillingManager` 由来の `MutableStateFlow`)、`ui/common/ProGate.kt`(`ProGate` = グレーアウト+金枠+左上「Pro」バッジ、`ProPaywallDialog` は価格+登録ボタン付き)。`ExportOptionsDialog` の非PDFチップと透かしON/OFF、**PDFパスワード保護**を `ProGate` で包み済み。**`PRO_GATING_ENABLED=true` にすればロックが出る**(項目2の残タスク参照)。追加候補の形式: HTML / 画像カード(PNG)
4c. **PDFパスワード保護(Pro機能・実装済み)**(2026-09-05): `export/PdfPasswordProtector.kt`(`com.tom-roush:pdfbox-android` 2.0.27.0)。`PdfExporter` が生成した PDF を PDFBox-Android で読み込み直し、`StandardProtectionPolicy`(ユーザー/所有者パスワード同一、AES-256)で暗号化して上書きする後処理方式。`ExportOptionsDialog` に PDF選択時のみ「パスワードを設定する」トグル(`ProGate`)+ パスワード入力欄(`PasswordVisualTransformation`)。`MeetingDetailViewModel.exportPdf` に `password: String?` / `onError` を追加(`runCatching` で暗号化失敗時は Toast)。**対象は PDF のみ**(Word/Excel/Markdown/CSV/.ics は OOXML暗号化の複雑さ・POI非使用のため技術的に非対応と判断)。APKサイズは PDFBox-Android 追加で約6MB増。パスワードはアプリに保存しない(毎回手入力)
5. PDF への画像ロゴ埋め込み(レイアウトは改善済み。テキストワードマークを画像に差し替え。アイコンSVGを流用可能)
6. **要約項目のプリセット化**(サブスク実装後・Pro機能想定): 現状は5項目固定(サマリー/決定事項/懸念点・注意点/ToDo/次回打ち合わせ)がWorkerのスキーマからRoomのカラム、表示、エクスポートまで全レイヤーにハードコード。「商談」「社内MTG」「採用面談」等のプリセットを切替できるようにする案。プリセットごとに固定スキーマを持てば prompt cache は維持可。DBは項目を可変にするため固定カラム→JSON1カラム等へ寄せる v6 マイグレーションが必要。自由入力のカスタム項目(動的スキーマ生成)はさらに大きいので当面対象外
7. **ネイビー配色の残り(2026-09-12〜13、Artifact「ネイビー基調のリデザイン案」より)**: 上記「配色」項の `ColorScheme` 化・`CreateActionAmber`・録音ボタン・`TabTopBar`/`FolderTabRow`/`ClientDetailScreen`ヘッダー/`HomeDashboardCard` のネイビー化、ページ地の淡いティール統一、下部ナビの白地化は実装済み。**(a) 録音3画面のネイビー+アンバー化・(b) セクション見出しのネイビー縦バー(`SectionHeading`、Home全ボード+分析全タブ+売上タブ)・(c) クライアント行の頭文字アバターも実装済み**(2026-09-13)。残る未実装(見た目の追加コンポーネント寄りで優先度低): (d) ホームダッシュボードの2枚カード分割(現状は1枚のネイビーカードに統合。`StatCell`3つは同カード内で白いチップとして表示、モックアップは別カードだが機能・データは同一) (e) 金額表示の薄紫ピル化 (f) Home画面の日付・挨拶・通知ベル等のヘッダー行は現状 `surface` のまま(モックアップではダッシュボードカードに内包)

## 既知の問題

- **【修正済み・実機再検証待ち】録音の音声認識エラー(`ERROR_CLIENT` / code=5)**: テスターの実機で発生(2026-08-31)。`TranscriptionManager` がセッション再開を `onEndOfSpeech` と `onResults`/`onError` で二重に行い、かつコールバック内から同期的に `startListening` していたのが原因。再開をメインHandler経由の遅延実行に一本化・多重起動ガード追加・code=5/8 は認識器を作り直して自動リトライ(連続5回超で打ち切り)に変更。エミュレータは日本語モデルが無く code=5 の再現不可のため、**テスターの実機での再テストが必要**
- **【修正済み・実機再検証待ち】下部ボタンがナビゲーションバーと重なる**: targetSdk 35+ の edge-to-edge 強制が原因。`ClientDetailScreen`(録音開始)/`ClientListScreen`/`ResultScreen` の `bottomBar` に `navigationBarsPadding()` を追加。エミュレータで解消を確認、実機(3ボタン/ジェスチャー両方)の確認待ち
- **【要再検証】実機での日本語音声認識の精度**: エミュレータは音声入力できないため未検証。テスターの実地確認が進行中
- **【要再現確認・2026-09-13報告】録音中、会話が途切れると文字起こしテキストがリフレッシュされる/停止後に文字起こしが空になる**: テスターより報告。`TranscriptionManager.kt`/`MeetingViewModel.kt` の文字起こし蓄積ロジック(`committedSegments`/`_transcript`)はこのセッションで変更しておらず(`RecordingScreen.kt` の色・レイアウトのみ変更)、コードレビューでは `start()` 時以外に `committedSegments`/`_transcript` をクリアする経路が見当たらない。エミュレータは日本語オンデバイス認識が無いため実機依存の挙動を再現できず未検証。次回は具体的な再現手順(無音の長さ、端末機種、対面/リモートいずれか)を確認して切り分ける
- **【未解決】PCスピーカーから出る相手の声が認識されない / 認識ごとの効果音**: `SpeechRecognizer` が自分でマイクを開くと近接音声向け処理(ノイズ抑制・AGC)が入り、離れた相手の声を削る。加えて多くの端末はマイクを開くたび効果音を鳴らす。試した対策と結果:
  - `EXTRA_AUDIO_SOURCE` に UNPROCESSED を指定(2026-09-06)→ extra が無視され効果なし → **revert 済み**
  - 自前 `AudioRecord` → `ParcelFileDescriptor` パイプ → `EXTRA_AUDIO_SOURCE` で recognizer に供給する `MicAudioFeeder`(2026-09-07)→ **オンデバイス認識器がパイプ入力を受け付けず、テキストが一切出なくなった(音声レベルだけ反応)ため revert**(`3299e34` 版に戻した)
  - 2026-09-07、`3299e34` ベースに (a) 録音中は `STREAM_MUSIC` の音量そのものを 0 に落として `stop()` で復元(`adjustStreamVolume(MUTE)` は SYSTEM/NOTIFICATION には DND 権限が無いと効かないため)。**元の音量を SharedPreferences(`audio_mute`)にも保存し、録音中にプロセスが落ちても次回起動時に `restoreLeftoverMediaVolume()`(`MeetingNotesApp.onCreate`)で復元** (b) `EXTRA_SPEECH_INPUT_COMPLETE_SILENCE_LENGTH_MILLIS` 等に 20秒を指定してセッションが沈黙で切れる頻度を下げる(端末が無視する場合あり) (c) `onPartialResults` を live 表示に反映。**効果音を端末レベルで完全に消すには DND 権限が必要で未実装**
- **診断ログ(2026-09-07)**: `util/DiagnosticsLog`(端末内・上限64KB・外部送信なし)。`MeetingNotesApp.onCreate` で `init()` + キャッチされない例外を既定ハンドラに委譲しつつ記録。`TranscriptionManager` が録音1回ごとに結果(長さ・文字数・再開回数・エラーコード別回数、正常終了か打ち切りか)を1行追記。ヘルプ画面(`HelpScreen`)末尾の「診断情報」カードから内容を「共有」/「消去」できる。音声・商談内容は含まない
- **CI(2026-09-07、2026-09-09 に修正)**: `.github/workflows/android.yml`(push: main/feature/**、PR: main → `testDebugUnitTest lintDebug assembleDebug`。Android SDK は runner 同梱、`local.properties` 不要)。**`gradlew` は git 上で mode 100755(実行可)にしておくこと** — 100644 だと Linux ランナーで `./gradlew` が exit 126(Permission denied)。`066d636` で `git update-index --chmod=+x gradlew` 済み。それ以前の Android CI は全 run が exit 126 で失敗していた(コードは無関係)。`MigrationIntegrityTest`(JVM)= `app/schemas` の最大バージョンを現行 DB バージョンとみなし、v5→現行まで各1段の Migration が `databaseMigrations` にあること・全て+1ステップ・重複なし・最終到達バージョン一致 を検証(実 SQL は androidTest の `MigrationTest`)
  - Zoom/Teams の文字起こし連携は不可(サードパーティにライブ字幕を公開していない)。`MediaProjection` の再生音キャプチャも VoIP 音声(`USAGE_VOICE_COMMUNICATION`)は仕様上キャプチャ不可。残る手は「自前 AudioRecord の PCM をアプリ内 STT(要ネット・プライバシー方針と非両立)」か「スピーカーフォン運用+マイクを近づける」の案内のみ
- (対応済み)DBマイグレーション: v5以降は正式Migrationを書く方式に変更済み(上記「アーキテクチャ・設計上の重要事項」参照)。v1〜v4からの更新のみ破壊的
