{
  "name": "meetingnotes-summary-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "商談メモ: 文字起こし→Claude要約の中継 Cloudflare Worker(APIキーをアプリから隠すため)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
